# TODO

Ideas that could be implemented.

## Remaining

Conformance backlog below is derived from `docs/conformance-audit-2026-07-15.md`,
a 2026-07-15 ground-truth audit covering all 76 OPC UA Foundation server
profiles/facets (492 CUs), not just the 4 canonical composite profiles
(123 CUs) previously tracked. See that doc for full per-theme evidence;
`specs/conformance-tester/CU-COVERAGE.md` has the per-CU ledger.

- ~~**Alarms & Conditions missing subtypes/properties**~~ — feature 095 (both passes) closed `TransitionTime`/`EffectiveTransitionTime`/`EffectiveDisplayName` on the core sub-states (14 of the 58-CU range — the remaining 44 are per-substate `Effective*` beyond `ActiveState` and per-specific-transition-edge timestamps, a materially larger increment, still open), all 10 Enable/Disable/Suppress/Unsuppress/RemoveFromService/PlaceInService/Silence Methods, A&C auditing for those Methods, and the full missing-alarm-subtype set: Level (`2746`/`3001`), Deviation (`2390`/`2951`, `DeviationAlarm` reusing `LimitAlarm`'s evaluator against `processValue-setpointValue`), RateOfChange (`2323`/`2946`, per-second rate over successive samples), `SystemOffNormalAlarmType` (`2239`, `DiscreteAlarmKind` variant), `CertificateExpirationAlarmType` (`2236`, `ExpirationDate`/`ExpirationLimit`), `DiscrepancyAlarmType` (`2861`, `TargetValueNode`/`ExpectedTime`/`Tolerance`, based directly on `AlarmConditionType` not `LimitAlarmType` per grounding), `OnDelay`/`OffDelay` (`2877`, `ConditionStateMachine::gate_active`), `ReAlarmTime`/`ReAlarmRepeatCount` (`2879`, resets on return-to-normal not acknowledge — corrected a wrong initial assumption via spec grounding), `AudibleSound`/`AudibleEnabled` (`2881`, server-computed from active/acked/silenced; found and fixed a real spec-conformance gap where Acknowledge didn't auto-silence). See `specs/095-ac-completion/tasks.md` T045-T052.
- **GDS Directory / Authorization / KeyCredential services**: `RegisterApplication`/`QueryServers`/`QueryApplications` are explicitly `BadServiceUnsupported` (not missing by accident); Authorization Service (OAuth2 token issuance) and KeyCredential Service don't exist at all; JWT/OAuth2 authority discovery (issuer endpoint URL) is absent. CUs `2232`, `2233`, `2709`, `2817`, `2902`, `3182`, `3581`, `3584`, `3586`, `5292`, `5293`, `5301`, `5302`, `5303`. Feature 104 found (informational only, doesn't close this item) that `RegisterApplication`/`QueryServers`/`QueryApplications`/`FindApplications`/`UpdateApplication`/`UnregisterApplication`/`GetApplication`'s real instance NodeIds are already known -- they live as `DirectoryType`-inherited methods on the same real "Directory" object (`ns=1;i=141` in the source XML) the Pull-model's `CertificateDirectoryType` methods resolve against.
- **GDS Push: DefaultHttpsGroup / DefaultUserTokenGroup TrustLists**: feature 102 (Run 2) closed CU `2231`'s TrustList surface for `DefaultApplicationGroup` only; `DefaultHttpsGroup`/`DefaultUserTokenGroup` CertificateGroups exist as separate, empirically-confirmed nodes in the generated nodeset (`ServerConfiguration_CertificateGroups_DefaultHttpsGroup` = 14088, `..._DefaultUserTokenGroup`) but this project's `CertificateStore` doesn't currently model separate per-group trust stores for them.
- **GDS Pull model Optional methods**: `RevokeCertificate`/`GetCertificates`/`CheckRevocationStatus` (Optional, CU 3582's Part 12 §7.9.6/§7.9.8/§7.9.11) remain unimplemented server-side -- feature 104 confirmed their real NodeIds already resolve (they're real instance methods on the same real Directory object), so the actual remaining gap is purely business-logic infrastructure: an issuance ledger (for `GetCertificates`), real CRL mutation (for `RevokeCertificate`), and a revocation-status lookup (for `CheckRevocationStatus`) -- none of which this SDK builds yet.
- **File Access: FileDirectoryType / TemporaryFileTransferType**: feature 106 closed the base `FileType` surface (CUs `3210`/`3213`) with real Open/Read/Write/Close/GetPosition/SetPosition I/O against the existing FOTA temporary file. `FileDirectoryType` (CU `3211`, OPC-10000-20 §4.3 -- `CreateFile`/directory browsing) and `TemporaryFileTransferType` (CUs `3810`-`3813`/`5791`, §4.4 -- `GenerateFileForRead`/`GenerateFileForWrite`/`CloseAndCommit`, a structurally distinct on-demand-file-generation pattern) remain unimplemented; both were explicitly out of scope for feature 106 (see `specs/106-file-access-io/spec.md` Assumptions) and need their own security-design pass before implementation -- `FileDirectoryType` in particular raises real path-traversal/arbitrary-filesystem-exposure questions a real implementation would need to resolve carefully (Security Is Paramount), unlike feature 106's narrower scope where the backing path is always operator-fixed, never client-derived.
- **RBAC RolePermissions Write path**: no live Write-service path sets `RolePermissions`/`DefaultRolePermissions` (only settable at node-creation/config time); `UserWriteMask` is static, never per-user computed. CUs `2806`, `2873`, `2163`, `3026`.
- **Historical structured-data update/delete**: both backends' `update_structure_data` accept only `Annotation`-typed values, contrary to what several CUs require. CUs `2185`, `2332`, `2664`, `2740`, `2937`, `3015`.
- **`ReadAnnotationDataDetails` continuation points rejected outright**: found incidentally while implementing feature 107 (`ReadAtTimeDetails`) -- both backends' `read_annotations` unconditionally return `Bad_ContinuationPointInvalid` for any supplied continuation point, but the real Part 11 v1.05.04 §6.5.6.2 text says "The standard ContinuationPoint rules (see 6.3) apply" to `ReadAnnotationDataDetails` too (the same class of mistake this feature's own planning almost made and corrected via spec re-verification -- see `specs/107-history-read-at-time/research.md` R9). Out of feature 107's scope (a different operation/CU); needs its own fix mirroring `read_at_time`'s index-based pagination approach.
- **Interpolated Aggregate doesn't search outward past Bad-quality points**: found while implementing feature 107 -- `aggregates::engine::agg_interpolative`/`compute_processed_intervals` derive interval-boundary `prior`/`next` values by nearest-neighbor scan only, with no quality filtering, unlike Part 13 §3.1.8's Interpolated Bounding Value definition (which requires searching outward past Bad-quality points). `ReadAtTimeDetails`'s own `use_simple_bounds=false` path implements the real outward-search correctly (see `specs/107-history-read-at-time/research.md` R3) but does not reuse/fix the aggregate engine's existing (narrower) behavior, which was out of scope for that feature.
- **Subscription Durability**: `SetSubscriptionDurable` is an inert generated nodeset entry; zero "durable" references in the server crate. CUs `3642`, `5795`.
- **Scheduler / Redundancy / Sessionless**: all three are unimplemented beyond generated stubs — Scheduler is not the same as the existing Part-10 `ProgramStateMachine`; Redundancy has no failover/clustering code; sessionless enforcement has an explicit TODO (`rbac/decision.rs:147`). CUs `4500`-`4503`, `2258`, `3027`, `3994`, `2871`.
- **PubSub MQTT config completion**: finish `specs/074-pubsub-gauntlet/tasks.md` T011/T012 by deriving QoS from `RequestedDeliveryGuarantee` and using configured `BrokerDataSetReaderTransportDataType.QueueName` topic filters instead of only writer/reader group IDs.
- **Conformance known partials**: close or document the remaining JSON/XML cross-stack vector strategy, FindServersOnNetwork mDNS counterparty validation, and audit-event coverage from `specs/conformance-tester/COVERAGE.md`. (Part 13 aggregate coverage: confirmed solid — 35/35 standard aggregates implemented and tested; Start/End variants and custom-aggregate extensibility are the only real gaps, see audit doc.)
- **CTT certification run**: run the demo server against the OPC Foundation Compliance Test Tool on Windows. See `docs/ctt-conformance.md`.
- ~~**Technical debt quick wins**: fix companion feature-name drift between `async-opcua-server/Cargo.toml` and `async-opcua-server/src/companion/mod.rs`; remove or narrow the `unexpected_cfgs` allowance; add a consistency check for declared companion features versus importer gates.~~
- ~~**Spec Kit reconciliation**: update `specs/069-companion-specs/tasks.md` and related plan/spec artifacts so they match the current companion-spec implementation strategy, or document the intended migration path if runtime XML import is temporary.~~
- ~~**Documentation cleanup**: update `docs/compatibility.md` so the crypto backend description matches the current `aws-lc-rs`/`rustls`/Rust-crypto implementation instead of stale OpenSSL wording.~~
- **Async lock audit**: audit synchronous lock use in session, subscription, secure-channel, and SQLite history paths; confirm lock guards are not held across `.await`; add targeted high-contention tests where needed.
- **Dependency debt review**: revisit `deny.toml` advisory exceptions and duplicate dependency stacks (`tokio-tungstenite`, `thiserror`, `rand`, `getrandom`, `rustix`, async runtime support crates); collapse versions where upstream compatibility allows.
- **Companion import strategy decision**: decide and document whether companion specs should use generated Rust, runtime XML imports, or a hybrid model; measure build time and footprint before enabling broad companion support.
- **Protocol TODO conversion**: turn high-impact protocol TODOs into grounded implementation tasks with OPC UA section references, including query partial-success behavior, `Variant::set_range_of` completeness, and event filter `RelatedTo` support.
- **Large-module refactoring backlog**: split the largest handwritten modules only around stable domain boundaries, starting with subscription service/client code, memory node manager internals, session manager, secure channel, and subscription state handling.
- **Debt KPI tracking**: track generated-code size, non-generated LOC hotspots, build time, binary footprint, dependency advisories, and feature-lattice failures as recurring technical debt metrics. See `docs/technical-debt-report-2026-07-07.md`.

## Done

- ~~Historical `ReadAtTimeDetails`~~ — feature 107: closes CU `3020` (OPC-10000-11 §6.5.5.2) and CU `2991` as a byproduct. `HistoryStorageBackend` gains two new default methods reused by both backends: `read_raw_reverse` (nearest sample at-or-before a timestamp, closest-first -- needed because `read_raw_modified` alone can't efficiently answer "nearest prior sample, however far back," and the in-memory backend silently ignores its own `return_bounds` flag, so relying on it as `read_processed` does would have broken cross-backend parity) and `read_at_time` (the real ReadAtTimeDetails logic: exact-timestamp match is `Raw`; otherwise interpolates between bounding values using `useSimpleBounds` to choose immediate-neighbor vs. bounded-outward-past-Bad-quality search, reusing the Interpolated Aggregate's own ratio-interpolation math (`interpolated_bound_at`, made `pub(crate)`) rather than duplicating it). Per-node `Stepped` configuration (already resolved via the existing `resolve_stepped` helper) selects step-hold (works for any `Variant` type, closing CU 2991 as a byproduct) vs. numeric sloped interpolation. Genuinely supports OPC UA's ContinuationPoint mechanism by paginating `req_times` in fixed batches -- a real correction made mid-implementation after re-verifying the spec text found "The standard ContinuationPoint rules apply," directly contradicting an earlier, wrong inference (by analogy with `read_processed`'s unconditional rejection) made during planning; see `specs/107-history-read-at-time/research.md` R9 for the full account. Proven end-to-end with a real client `HistoryRead(ReadAtTimeDetails)` Call against real recorded history. See `specs/107-history-read-at-time/`.
- ~~File Access real I/O (FileType Open/Read/Write/Close)~~ — feature 106: closes CUs `3210`/`3213` (base `FileType`, OPC-10000-20 §4.2 -- grounded against the local Part 20 PDF; `FileType` is defined there, not Part 5 as its name might suggest, which only has a subtype). `fota/file_node.rs` already built a structurally-correct `FileType` object (session-bound temporary firmware-upload file) with real Method nodes, but nothing registered callbacks against them. New `fota/file_access.rs` wires real behavior against the existing FOTA file: `Open` (mode-byte decode, exact spec open-conflict rules re-derived from the real text -- a write-open is refused while the file is open in *any* mode, a read-open only while open for write), `Close`, `Read` (EOF returns empty, not an error), `Write` (empty data is a no-op), `GetPosition`/`SetPosition` (clamps beyond EOF), all against a real `std::fs::File` per session-scoped handle -- a `moka::sync::Cache` registry directly modeled on `gds/trust_list`'s `TrustListHandleRegistry` but disk-backed rather than in-memory (appropriate for large files like firmware images, and avoids one client being able to force unbounded server memory use). `MaxByteStringLength` bounds both `Read` and `Write` per call. Status codes independently re-verified against the real spec text (e.g. `Bad_InvalidArgument` for any bad/foreign-session handle -- corrected from `TrustListHandleRegistry`'s own `Bad_InvalidState` convention, which turned out to be a Part-12-specific choice, not the base FileType spec's). Proven end-to-end: a real client `Open`s for write, `Write`s bytes, `Close`s, then in a separate `Open`/`Read`-loop/`Close` sequence gets the exact same bytes back, through real Call-service dispatch against a real `SimpleNodeManager` (not `CoreNodeManager` -- FOTA's namespace is an ordinary operator-owned application namespace, not a companion/core one). `FileDirectoryType` (CU `3211`) and `TemporaryFileTransferType` (CUs `3810`-`3813`/`5791`) are explicit, separately-scoped follow-ups requiring their own security-design pass -- tracked above. See `specs/106-file-access-io/`.
- ~~GDS Pull model client-side fix (Run 2)~~ — feature 105: `async-opcua-client/src/gds/` had the exact same fabricated-NodeId defect feature 103 (Run 1) fixed server-side (`directory_object_id=22384`, `register_method_id=22385`, `certificate_manager_id=22388`, `start_signing_request_id=22400`, `finish_signing_request_id=22402`). Since this client talks to an *external* real GDS product, hardcoded constants can never be correct -- replaced with real dynamic discovery: `GdsClient::discover(session)` reads the target server's namespace array (`Session::get_namespace_index`) to find the GDS companion namespace's actual index, then resolves the Directory object and its `RegisterApplication`/`StartSigningRequest`/`FinishRequest` methods via `TranslateBrowsePathsToNodeIds` (OPC-10000-4 §5.8.4) -- both client APIs already existed, no new session capability needed. Also corrected `GdsCsrClient::certificate_manager_id` -> `directory_object_id` (there is no separate "CertificateManager" object per Part 12 §7.9.2 and this SDK's own corrected server-side model, feature 104) and removed a bogus 5th argument (`regenerate_private_key`) `start_signing_request` sent that doesn't exist in the real `StartSigningRequest` signature (re-verified: 4 InputArguments only). Proven end-to-end against this SDK's own server with the GDS namespace at a non-default index. This investigation found and fixed two more previously-undiscovered server-side infrastructure bugs (server-side was otherwise untouched): (1) `Server_NamespaceArray` (the array remote clients read to discover namespace indices) never reflected a runtime-imported namespace, since `InMemoryNodeManager::namespaces_for_user` delegated to each node-manager impl's own *static* namespace list rather than the live, refreshable cache feature 103 already built -- fixed by merging the two; (2) `AddressSpace`'s namespace table and `context.type_tree`'s independent namespace table (which `DiagnosticsNodeManager` uses to claim the server's own application namespace index at startup) can silently collide, since the companion import's namespace-seeding (feature 103) only consulted the former -- fixed by having `register_gds_pull_methods_from_companion` (now taking a `type_tree` parameter) reconcile the two before importing. See `specs/105-gds-pull-client-fix/`.
- ~~GDS Pull Directory Singleton Correction (Run 1 rework)~~ — feature 104: corrected feature 103's wrong research conclusion that `CertificateDirectoryType` ships no pre-instantiated singleton in the GDS companion NodeSet -- the real NodeSet2.xml ships a fully pre-instantiated "Directory" object (source `ns=1;i=141`) with real integer NodeIds for every Mandatory method and the `CertificateGroups`/`DefaultApplicationGroup`/`TrustList` subtree. Feature 103 had built ~250 lines of custom object-instantiation logic to hand-construct a *duplicate* "Directory" object with fabricated string NodeIds; rewrote `gds/directory_instance.rs` to simply resolve the real object instead (net code deletion). Added a regression test (`does_not_construct_a_duplicate_directory_object`) guarding against reintroducing the duplicate. Also corrected the CU-number citation in feature 103's original spec (2230 -> the actual register entry 3582) and the Optional-methods deferral reasoning (real NodeIds now resolve; ledger/CRL-mutation business logic is the actual remaining gap). See `specs/104-gds-pull-directory-fix/`.
- ~~GDS Pull model fix (Run 1)~~ — feature 103: fixed CU `3582` (mis-cited as "CU 2230" in the original speckit spec -- corrected here). `gds/pull_methods.rs` previously implemented the wrong (Push-model) methods against fabricated NodeIds, the same defect class as pre-fix `push_methods.rs`. `CertificateDirectoryType` (Part 12 §7.9.2) exists only in the GDS companion NodeSet2.xml and ships with no pre-instantiated singleton (unlike `ServerConfigurationType`), so this run also activated the previously-entirely-dormant companion-NodeSet-import subsystem (`async-opcua-server::companion`, made `pub`) and built new object-instantiation logic (`gds/directory_instance.rs`) scoped narrowly to `CertificateDirectoryType`'s known Mandatory shape: a "Directory" object plus its six Mandatory methods and a `CertificateGroups`/`DefaultApplicationGroup`/`TrustList` subtree (reusing core namespace-0 types). Rewrote `gds/pull_methods.rs` entirely against the dynamically-resolved NodeIds: `StartSigningRequest`/`StartNewKeyPairRequest` (new `X509::issue_certificate_for_public_key` CA-issuance primitive -- this server acting as issuer for a third party's public key, not self-signing), `FinishRequest`, `GetCertificateGroups`, `GetTrustList`, `GetCertificateStatus`. Proven end-to-end via a real Call-service dispatch test (`gds_pull_companion_integration.rs`) plus 12 unit tests. This investigation found and fixed three independent, previously-undiscovered bugs in shared node-manager infrastructure well outside this feature's own new files: (1) `import_companion_xml` seeded a disconnected, empty `NamespaceMap` instead of the address space's real registered namespaces, risking silent namespace-index collisions on any real (non-fresh) server; (2) `InMemoryNodeManager::owns_node` checked a namespace-set snapshot frozen at construction time that never refreshed for namespaces added by a later runtime import, so Call/Read/Write/Browse dispatch would never recognize a companion-imported namespace as owned (fixed via a new `refresh_namespaces()` method); (3) `CoreNodeManagerImpl::call_builtin_method`, under the default-on `subscriptions-standard` feature, unconditionally short-circuited before ever consulting the generic method-callback registry for any method outside the core namespace-0 `MethodId` enum -- silently making any custom callback registered for a companion-spec (or other custom-namespace) method unreachable. Client-side `async-opcua-client/src/gds/` has the same fabricated-NodeId defect and needs dynamic Browse-based discovery instead (a real external GDS deployment assigns its own namespace index) -- explicit Run 2 follow-up, tracked above. See `specs/103-gds-pull-fix/`.
- ~~GDS Push TrustList completion (Run 2)~~ — feature 102: closes the remaining `TrustListType`/CertificateGroup surface of CU `2231` for `DefaultApplicationGroup`, deferred by feature 101. New `gds/trust_list.rs` implements `Open`/`OpenWithMasks`/`Read`/`Write`/`GetPosition`/`SetPosition`/`Close`/`CloseAndUpdate`/`AddCertificate`/`RemoveCertificate` against real, empirically-verified NodeIds (`TrustList` object `ns=0;i=12642` and its 10 methods). Session-scoped file handles use a `moka::sync::Cache` with `time_to_idle` (not `time_to_live`, since Part 12's `ActivityTimeout` resets on each call rather than from creation) modeled on the existing `HistoryContinuationPointCache` pattern. `CloseAndUpdate` extends Run 1's `PushTransaction`/`ApplyChanges`/`CancelChanges` to also carry a pending TrustList change alongside the existing pending certificate/key change. `CertificateStore` gained write-side helpers (`store_trusted_cert`/`store_issuer_cert`/`remove_trusted_cert`/`remove_issuer_cert`/`store_trusted_crl`/`store_issuer_crl`/`replace_trusted_certs`/etc.) reusing the generated `TrustListDataType` binary codec and `X509::thumbprint()`. `RemoveCertificate`'s "still a needed CA" check is name-based (issuer/subject string matching), a documented simplification short of full chain validation. Also fixed a latent, pre-existing gap found while extending `gds_push_integration.rs`: that file's own feature gate was missing `generated-address-space`, which its Run-1-era `CoreNodeManager` import already implicitly required. `DefaultHttpsGroup`/`DefaultUserTokenGroup` and the CU `3582` Pull-model sibling bug remain explicit follow-ups. See `specs/102-gds-push-trustlist/`.
- ~~GDS Push model fix + completion (Run 1)~~ — feature 101: found and fixed a real bug in `gds/push_methods.rs` (CU `2231`) -- its `CreateSigningRequest`/`CertificateManager` NodeId constants were fabricated, resolving to unrelated Alarms & Conditions properties rather than any real node, and it also registered against the wrong node manager type entirely (`SimpleNodeManager` instead of `CoreNodeManager` -- `ServerConfiguration` is a namespace-0 node). Also removed a misplaced Pull-model method (`StartSigningRequest`, OPC-10000-12 §7.9.3, which never belonged in the Push-model file). Implemented the real `ServerConfigurationType` Push-model methods against verified NodeIds: `CreateSigningRequest` (real PKCS#10 CSR generation via `x509_cert::builder::RequestBuilder`), `UpdateCertificate`/`ApplyChanges`/`CancelChanges` (single-transaction lifecycle, §7.10.2, committing via the already-built `gds_reload::save_new_credentials`+hot-reload), `GetRejectedList`, `ResetToServerDefaults` (reuses feature 097's shutdown-scheduling mechanism). `CreateSelfSignedCertificate`/`DeleteCertificate`/`GetCertificates` confirmed absent from the generated nodeset -- documented as a nodeset-source gap. Proven end-to-end with a real Call-service dispatch test (`gds_push_integration.rs`) plus 15 unit tests. TrustList/CertificateGroup methods (Run 2) and the sibling `pull_methods.rs`/CU `3582` bug are explicit, tracked follow-ups. See `specs/101-gds-push-fix/`.
- ~~Data Access conformance completion~~ — feature 100: `async-opcua-server/src/data_access.rs` closes 9 CUs across the DiscreteItemType family (`TwoStateDiscreteType` `2361`, `MultiStateDiscreteType` `2988`, `MultiStateValueDiscreteType` `2831`, `DiscreteItemType` `2426` as an abstract-base byproduct) and the full `ArrayItemType` family (`YArrayItemType` `3323`, `XYArrayItemType` `3324`, `ImageItemType` `3325`, `CubeItemType` `3326`, `NDimensionArrayItemType` `3327`). `MultiStateDictionaryEntryDiscreteBaseType`/`ValueAsDictionaryEntries` (`2474`/`2776`) investigated and explicitly deferred — the type exists in the generated core nodeset but is undocumented in both the local Part 8 v1.05.07 PDF and reference.opcfoundation.org, likely a schema-only addition ahead of published spec text; implementing against unverifiable semantics was rejected per this project's correctness-first principle. Real implementation finding: `NDimensionArrayItemType` (ValueRank=0) is the one ArrayItem subtype whose validity check compares `ArrayDimensions` against the Variant value's own multi-dimensional shape (`Array::new_multi`), unlike the fixed-rank subtypes. See `specs/100-data-access-completion/`.
- ~~Session Cancel service completion~~ — feature 099: closes CU `2190`, the one required-CU item explicitly flagged as needing real architectural work. Cancel now reaches into `SessionSubscriptions::publish_request_queue` (the one request type this server holds outstanding for any meaningful duration) and resolves matching queued Publish requests with `Bad_RequestCancelledByClient` (OPC-10000-4 §5.7.5), routed through the existing per-session subscription actor's command pattern. Verified with a real end-to-end test that queues a raw `Publish` request, cancels its exact `requestHandle`, and confirms it resolves with the correct fault instead of its normal timeout. See `specs/099-session-cancel/`.
- ~~Address Space Write enforcement completion~~ — feature 098: closes the "Attribute Write remaining gaps" backlog entry (3 CUs). `WriteFullArrayOnly` (CU `2820`) is now enforced — an IndexRange Write to a Variable declaring it is rejected with `Bad_WriteNotSupported` (Part 3 §8.58 Table 42, Part 4 §5.11.4 Table 53); `2936` (StatusCode & Timestamp write) closed with a round-trip test that found and fixed two real bugs in `write_node_value`: `server_timestamp` was hardcoded to `now()` instead of honoring the client-supplied value, and the ByteString→byte-array coercion `Variable::set_value` has was missing from the Write-service path entirely; `4237` (NonVolatile/Constant) closed via targeted test — the underlying `AccessLevelEx` bitmask plumbing already worked, no code gap. (IndexRange writes via `Variant::set_range_of`, CU `3147`, was already done.) See `specs/098-write-enforcement/`.
- ~~Base Info conformance completion~~ — feature 097: instantiation helpers (`async-opcua-server/src/base_info.rs`) for `OrderedListType`/`IOrderedObjectType` (CU `2512`, whose `HasInterface` wiring also closes CU `3560` Address Space Interfaces as a byproduct — found during implementation that this address space's reference storage is a `HashSet`, so Browse order cannot be relied on for `HasOrderedComponent`; `NumberInList` is the authoritative order signal, matching OPC-10000-5 §6.11's own stated rationale for that property), `SelectionListType` (CU `2711`), `OptionSetType` (CU `3127`), `ValueAsText` (CU `2969`), `ReferenceDescriptionVariableType`/`HasReferenceDescription` (CU `3996`, defined in OPC-10000-23 — a companion part not previously cached locally), and the `CurrencyUnit` property (CU `5240`); `EstimatedReturnTime` (CU `3198`) extends the server's existing `schedule_shutdown` mechanism rather than adding a new one. See `specs/097-base-info-completion/`.
- ~~Server Capabilities & Diagnostics conformance completion~~ — feature 096: wired the remaining unwired `ServerCapabilities` Max* nodes (`MaxSessions` CU `3912`; `MaxMonitoredItemsQueueSize`/`MaxMonitoredItemsPerSubscription`/`MaxSubscriptionsPerSession`/server-wide `MaxSubscriptions`+`MaxMonitoredItems`=0 CU `3911`) to their existing config fields; documented (not built) why `SamplingIntervalDiagnosticsArray` is correctly absent — CU `3196` is conditional on a fixed sampling-interval set per OPC-10000-5 §7.9/§12.8, and this server negotiates continuously-variable intervals, so exposing the array would itself be non-conformant; added `docs/server-capacity-limits.md` (CU `3808`); proved the already-wired `Locations` object resolves via Browse (CU `4053`, no code fix needed). See `specs/096-server-capabilities-diagnostics/`.
- ~~Time Sync profile decision~~ — feature 093: `TimeSyncSource` extensibility trait; built-in `OsClockSource` default (CU 2478) + configurable `max_acceptable_clock_skew` (CU 3802) + opt-in `UaHeaderTimeSyncSource` poller (CU 5505, `time-sync-ua` feature); CU 5793 closes as a consequence. PTP/gPTP/NTP (CU 2479/2480/2786) documented as a user-supplied `TimeSyncSource` extension point rather than implemented in-library, grounded in OPC-10000-84 §6.6.3.6. See `docs/time-synchronization.md`.
- ~~CU registry and evidence report~~ — `tools/cu-coverage-report` + `specs/conformance-tester/CU-COVERAGE.md`: in-repo CU-indexed registry from the OPC UA Foundation profile snapshot, covering Nano/Micro/Embedded/Standard 2025 server profile closures with `implemented`/`partial`/`needs-proof`/`extensible`/`source-issue` evidence labels (PR #294; `extensible` added in feature 093).
- ~~Shrink foundation profile footprints~~ — feature 066: nano 12M→6.8M, micro 13M→7.3M, embedded 26M→17M (43-45% reduction via opt-level=z + LTO + strip).
- ~~Kerberos SSO: keytab path plumbing~~ — feature 065.
- ~~Kerberos SSO: integration test & CI KDC setup~~ — feature 065.
- ~~Kerberos SSO: GssapiIdentityValidator, feature flag, builder API, IssuedToken dispatch, role mapping~~ — feature 064.
- ~~Replace per-request timers with shared deadline queue~~ — feature 063 (US3).
- ~~Cache session Arc in request dispatch context~~ — feature 063 (US2).
- ~~Investigate ArcSwap debt overhead~~ — feature 063 (US4): 3 of 4 ArcSwap instances were startup-only and replaced with plain `Arc<T>`.
- ~~Split AddressSpace hot/cold: expose DashMap directly for reads~~ — feature 063 (US1).
- ~~Flesh out the server and client SDK with tooling for ease of use.~~ — feature 058 (QuickNodeManager builder API).
- ~~Make it even easier to implement custom node managers.~~ — feature 058.
- ~~RSA-KEM encrypted UserName token integration test~~ — feature 058.
- ~~Embedded profile secure channel smoke test~~ — feature 058.
- ~~Standard profile X509/RegisterServer2 tests~~ — feature 058.
- ~~Throughput benchmark regression: investigate and restore performance baseline.~~ — features 060 (compilation optimization, +11%) and 061 (hot-path audit fixes, allocation/caching/validation).
