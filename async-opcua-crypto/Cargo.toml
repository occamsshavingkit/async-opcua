[package]
name = "async-opcua-crypto"
version = "0.19.0"
description = "OPC UA cryptography library"
authors = ["Adam Lock <locka99@gmail.com>", "Einar Omang <einar@omang.com>"]
homepage = "https://github.com/freeopcua/async-opcua"
repository = "https://github.com/freeopcua/async-opcua"
license = "MPL-2.0"
keywords = ["opcua", "opc", "ua"]
categories = ["network-programming"]
readme = "README.md"
documentation = "https://docs.rs/async-opcua-crypto/"
edition = "2021"
publish = true

[lints]
workspace = true

[lib]
name = "opcua_crypto"

[dependencies]
chrono = { workspace = true }
gethostname = { workspace = true }
num-bigint-dig = { workspace = true }
base64 = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

async-opcua-types = { path = "../async-opcua-types", version = "0.19.0" }

aes = { workspace = true }
aws-lc-rs = { version = "1.17.0", optional = true }
cbc = { workspace = true }
ctr = { workspace = true }
const-oid = { workspace = true }
# `der` is required directly so the `Sequence` derive macro can be invoked for
# the manual OCSPRequest encoder (T007); the same `der` 0.7.x release is
# already pulled in transitively by `x509-cert` 0.2.5.
der = { version = "0.7.6", default-features = false, features = ["alloc", "derive", "oid"] }
ecdsa = { workspace = true, optional = true }
hkdf = { workspace = true, optional = true }
hmac = { workspace = true }
p256 = { workspace = true, optional = true }
p384 = { workspace = true, optional = true }
rand = { workspace = true }
rsa = { workspace = true }
sha1 = { workspace = true }
sha2 = { workspace = true }
subtle = "2.6.1"
x509-cert = { workspace = true }
zeroize = { workspace = true }
x509-ocsp = { version = "0.2.1", default-features = false, features = ["std", "builder"] }
ureq = { version = "3", default-features = false, features = ["_tls"] }

[dev-dependencies]
tempfile = "3"

[features]
default = ["aws-lc-rs", "ecc"]
aws-lc-rs = ["dep:aws-lc-rs"]
ecc = ["dep:ecdsa", "dep:hkdf", "dep:p256", "dep:p384"]
legacy-crypto = []
