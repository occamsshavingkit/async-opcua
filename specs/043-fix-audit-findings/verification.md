# Verification Log: Audit Findings Remediation

Use this log as the per-task evidence ledger for feature 043. Do not check off a task in
`tasks.md` until this log records evidence for that task: failing-test evidence where applicable,
the implementation command or action, the final verification command and result, and any residual
risk. Use `N/A` only when the task is explicitly verification-only, documentation-only, or
not-a-bug; otherwise replace `Pending` with concrete evidence.

| Task ID | Failing-test evidence | Implementation command | Final command | Residual risk |
|---------|-----------------------|------------------------|---------------|---------------|
| T001 | N/A setup traceability artifact | Created `specs/043-fix-audit-findings/finding-matrix.md` with 168 task rows | Matrix row count matched 168 task ids and spot-check rows T001/T002/T003a/T007a/T021a/T048a/T101a/T106j/T108 were present | Exact StatusCode cells intentionally remain `TBD before implementation` where the task text does not name a code |
| T002 | N/A setup verification artifact | Created `specs/043-fix-audit-findings/verification.md` with 168 task rows | Verification row count matched 168 task ids and command-only rows T021a/T048a/T101a/T106j were prefilled | Evidence cells remain `Pending` until each individual task runs |
| T003a | N/A foundational shared assertion helper | Added and used `assert_activate_session_status` in `async-opcua-server/tests/security_tests.rs` | `cargo test -p async-opcua-server --test security_tests activate_session_status_assertion_reports_mismatch` passed | Helper is test-only; broader `security_tests --no-run` was run by worker and passed |
| T003b | N/A foundational shared assertion helper | Added `assert_activate_session_identity_unchanged` in `async-opcua-server/tests/security_tests.rs` | `cargo test -p async-opcua-server --test security_tests activate_session_identity_unchanged_assertion_reports_mutation` passed | Helper is test-only; broader `security_tests --no-run` was run by worker and passed |
| T003c | N/A foundational shared assertion helper | Added `assert_activate_session_no_certificate_audit_before_authentication` in `async-opcua-server/tests/security_tests.rs` | `cargo test -p async-opcua-server --test security_tests activate_session_no_certificate_audit_assertion_reports_emission` and `cargo test -p async-opcua-server --test security_tests x509_user_token_bad_signature_is_distinguishable` passed | Helper currently accepts caller-provided audit counts; later audit-capture tests must feed real counts |
| T004a | N/A foundational shared assertion helper | Added `assert_malformed_uadp_datagram_decode_rejected` in `async-opcua-pubsub/tests/pubsub_tests.rs` | `cargo test -p async-opcua-pubsub --test pubsub_tests uadp_dataset_message_rejects_field_count_above_decoding_limit` passed | Helper is test-only; broader `pubsub_tests --no-run` was run by worker and passed |
| T004b | N/A foundational shared assertion helper | Added `assert_malformed_uadp_datagram_subscriber_state_unchanged` in `async-opcua-pubsub/tests/pubsub_tests.rs` | `cargo test -p async-opcua-pubsub --test pubsub_tests uadp_dataset_message_rejects_field_count_above_decoding_limit` passed | Helper is test-only; broader `pubsub_tests --no-run` was run by worker and passed |
| T005a | N/A foundational shared snapshot helper | Added `CertificateMaterialSnapshot` helpers in `async-opcua-server/tests/gds_pull_methods.rs` | `cargo test -p async-opcua-server --test gds_pull_methods` passed | Helper captures known finished signing requests by explicit id list; later tests must pass ids they need to compare |
| T005b | N/A foundational shared assertion helper | Added `assert_certificate_material_unchanged` in `async-opcua-server/tests/gds_pull_methods.rs` and used it on an UpdateCertificate failure path | `cargo test -p async-opcua-server --test gds_pull_methods` passed | Helper compares known finished signing requests by explicit id list; later tests must include ids for material they expect to preserve |
| T006 | N/A foundational matrix gate | Added `MCP Citation Check` section to `specs/043-fix-audit-findings/finding-matrix.md` | `rg -n "MCP Citation Check|TBD before implementation|OPC UA document" specs/043-fix-audit-findings/finding-matrix.md` passed and matrix row count remained 168 | Tasks with `TBD before implementation` still require MCP resolution before their production code changes |
| T007a | `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_mode_mismatch_before_authentication` failed as expected because ActivateSession returned `Good` instead of `BadSecureChannelIdInvalid` | Added red SecurityMode mismatch test in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1 and 7.38.2 | Expected-red command recorded; T014a must make it pass | Production behavior not changed; implementation remains pending in T014a |
| T007b | `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_policy_mismatch_before_authentication` failed as expected because ActivateSession returned `Good` instead of `BadSecureChannelIdInvalid` | Added red SecurityPolicy mismatch test in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1 and 7.38.2 | Expected-red command recorded; T014b must make it pass | Production behavior not changed; implementation remains pending in T014b |
| T008 | `cargo test -p async-opcua-server activate_session_rejects_anonymous_transfer_to_new_sign_channel_before_authentication` failed as expected because ActivateSession returned `Good` instead of `BadIdentityTokenRejected` | Added red anonymous Sign-only transfer test in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1, 5.7.3.3, and 7.38.2 | Expected-red command recorded; T015 must make it pass | Production behavior not changed; implementation remains pending in T015 |
| T009 | `cargo test -p async-opcua-server --test security_tests username_password_token_requiring_protection_rejects_cleartext_before_authentication` failed as expected because authentication was reached once before rejection | Added red username/password token protection test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 7.40.4, 7.41, 7.40.2.1, and 7.38.2 | Expected-red command recorded; T016 must make it pass | Production behavior not changed; implementation remains pending in T016 |
| T010 | `cargo test -p async-opcua-server --test security_tests issued_token_requiring_protection_rejects_cleartext_before_claim_validation` failed as expected because cleartext issued token reached validation and returned `BadUserAccessDenied` instead of `BadIdentityTokenInvalid` | Added red issued-token protection test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 7.40.6, 7.41, 7.40.2.1, and 7.38.2 | Expected-red command recorded; T017 must make it pass | Production behavior not changed; implementation remains pending in T017 |
| T011 | `cargo test -p async-opcua-server --test security_tests x509_user_token_legacy_signature_is_rejected_when_enhanced_proof_required` failed as expected because legacy X.509 proof unexpectedly succeeded instead of `BadUserSignatureInvalid` | Added red enhanced channel-bound X.509 proof test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 6.1.8, 7.40.5, and 5.7.3.3 | Expected-red command recorded; T018 must make it pass | Production behavior not changed; implementation remains pending in T018 |
| T012 | `cargo test -p async-opcua-server --test security_tests x509_user_token_missing_signature_is_user_signature_invalid` passed in the current tree | Added lock-in X.509 missing `userTokenSignature` test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 5.7.3.3 and 7.40.5 | Already passing, so no red defect remains for missing X.509 proof status; `rustfmt --edition 2021 --check async-opcua-server/tests/security_tests.rs` and `git diff --check -- async-opcua-server/tests/security_tests.rs` passed in worker | T019 still owns production mapping for malformed X.509 `userTokenSignature` and broader signature-status behavior |
| T013 | `cargo test -p async-opcua-server activate_session_failed_x509_activation_does_not_leave_rejected_identity_state` passed in the current tree | Added lock-in X.509 failed-activation state cleanup test in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.2 and 5.7.3.3 | Already passing, so this specific failed X.509 activation path already preserves prior session identity and allows later valid activation | T020 still owns broader delayed identity/roles/claims assignment for all failed activation preconditions |
| T014a | `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_mode_mismatch_before_authentication` passed; related policy mismatch test still failed as expected with `Good` | Added pre-auth cross-channel `SecurityMode` comparison in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1 and 7.38.2 | SecurityMode mismatch now returns `BadSecureChannelIdInvalid` before authentication; worker `rustfmt --edition 2021 --check async-opcua-server/src/session/manager.rs` and `git diff --check -- async-opcua-server/src/session/manager.rs` passed | T014b still owns SecurityPolicy equality |
| T014b | `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_policy_mismatch_before_authentication` and `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_mode_mismatch_before_authentication` passed | Added pre-auth cross-channel `SecurityPolicy` comparison in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1 and 7.38.2 | SecurityPolicy mismatch now returns `BadSecureChannelIdInvalid` before authentication while preserving the T014a SecurityMode check; worker `rustfmt --edition 2021 --check async-opcua-server/src/session/manager.rs` and `git diff --check -- async-opcua-server/src/session/manager.rs` passed | T015 still owns anonymous transfer over new Sign-only channels |
| T015 | `cargo test -p async-opcua-server activate_session_rejects_anonymous_transfer_to_new_sign_channel_before_authentication`, `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_mode_mismatch_before_authentication`, and `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_policy_mismatch_before_authentication` passed | Added pre-auth anonymous-on-new-Sign-channel rejection in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.1, 5.7.3.3, 7.38.2, and 7.40.2.1 | Anonymous activation over a new Sign-only SecureChannel is rejected with `BadIdentityTokenRejected` when the existing session identity is non-anonymous; worker `rustfmt --edition 2021 --check async-opcua-server/src/session/manager.rs` and `git diff --check -- async-opcua-server/src/session/manager.rs` passed | T016-T020 still own token-protection, X.509 signature selection, and broad failed-activation state cleanup |
| T016 | `cargo test -p async-opcua-server --test security_tests username_password_token_requiring_protection_rejects_cleartext_before_authentication` passed; `cargo test -p async-opcua-server --test security_tests issued_token_requiring_protection_rejects_cleartext_before_claim_validation` still failed as expected with `BadUserAccessDenied` | Added username/password token protection validation in `async-opcua-server/src/session/negotiate.rs`, advertised explicit username token policy URI in `async-opcua-server/src/authenticator.rs`, and wired the validation before password decoding/authentication in `async-opcua-server/src/info.rs`; MCP refs OPC-10000-4 7.40.4, 7.41, 7.40.2.1, 7.38.2, and 5.7.3.3 | Cleartext username/password credentials requiring token protection now return `BadIdentityTokenInvalid` before authenticator side effects; worker `rustfmt --edition 2021 --check async-opcua-server/src/session/negotiate.rs async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` and `git diff --check -- async-opcua-server/src/session/negotiate.rs async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` passed | T017 still owns issued-token protection before claim validation |
| T017 | `cargo test -p async-opcua-server --test security_tests issued_token_requiring_protection_rejects_cleartext_before_claim_validation` and `cargo test -p async-opcua-server --test security_tests username_password_token_requiring_protection_rejects_cleartext_before_authentication` passed | Added issued-token protection validation/decrypt helpers in `async-opcua-server/src/session/negotiate.rs` and wired them before JWT/claim validation in `async-opcua-server/src/info.rs`; `async-opcua-server/src/authenticator.rs` still carries the T016 username policy URI change; MCP refs OPC-10000-4 7.40.6, 7.41, 7.40.2.1, 5.7.3.3, and 7.38.2 | Cleartext issued tokens requiring protection now return `BadIdentityTokenInvalid` before claim validation while preserving T016 username/password behavior; worker `rustfmt --edition 2021 --check async-opcua-server/src/session/negotiate.rs async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` and `git diff --check -- async-opcua-server/src/session/negotiate.rs async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` passed | T018-T020 still own X.509 signature proof and broader failed-activation state handling |
| T018 | `cargo test -p async-opcua-server --test security_tests x509_user_token_legacy_signature_is_rejected_when_enhanced_proof_required`, `cargo test -p async-opcua-server --test security_tests x509_user_token_missing_signature_is_user_signature_invalid`, `cargo test -p async-opcua-server --test security_tests username_password_token_requiring_protection_rejects_cleartext_before_authentication`, and `cargo test -p async-opcua-server --test security_tests issued_token_requiring_protection_rejects_cleartext_before_claim_validation` passed | Added X.509 user-token signature selection in `async-opcua-server/src/authenticator.rs` and routed X.509 proof validation through it in `async-opcua-server/src/info.rs`; MCP refs OPC-10000-4 6.1.8, 7.40.5, 5.7.3.3 and OPC-10000-6 6.1/6.7.5 | Enhanced policies now reject the legacy proof path with `BadUserSignatureInvalid`; legacy verification remains for non-enhanced policies; worker `rustfmt --edition 2021 --check async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` and `git diff --check -- async-opcua-server/src/authenticator.rs async-opcua-server/src/info.rs` passed | Full positive acceptance of a channel-bound enhanced proof still needs channel thumbprint/client-nonce context to be passed into the X.509 verifier |
| T019 | `cargo test -p async-opcua-server --test security_tests x509_user_token_missing_signature_is_user_signature_invalid`, `cargo test -p async-opcua-server --test security_tests x509_user_token_bad_signature_is_distinguishable`, and `cargo test -p async-opcua-server --test security_tests x509_user_token_legacy_signature_is_rejected_when_enhanced_proof_required` passed | Added explicit null/empty X.509 `userTokenSignature.signature` mapping to `BadUserSignatureInvalid` in `async-opcua-server/src/authenticator.rs`; malformed/invalid non-empty signatures still normalize through verifier error mapping; MCP refs OPC-10000-4 5.7.3.2, 5.7.3.3, 6.1.8, and 7.40.5 | Missing, empty, malformed, and invalid X.509 user-token signatures now surface the precise user-signature status while preserving T018 enhanced-policy rejection | T020 still owns delayed session identity/roles/claims assignment across all failed activation preconditions |
| T020 | `cargo test -p async-opcua-server activate_session_failed_x509_activation_does_not_leave_rejected_identity_state`, `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_mode_mismatch_before_authentication`, `cargo test -p async-opcua-server activate_session_rejects_cross_channel_security_policy_mismatch_before_authentication`, and `cargo test -p async-opcua-server activate_session_rejects_anonymous_transfer_to_new_sign_channel_before_authentication` passed | Delayed destructuring and assignment of authenticated `user_token`/claims until after late activation checks in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.7.3.2 and 5.7.3.3 | Failed late activation preconditions drop pending authentication state without calling `session.activate`; `rustfmt --edition 2021 --check async-opcua-server/src/session/manager.rs` and `git diff --check -- async-opcua-server/src/session/manager.rs` passed | US1 implementation complete; T021a-T021c checkpoint tests remain |
| T021a | `cargo test -p async-opcua-server security_tests` passed but ran 0 tests because Cargo treated `security_tests` as a test-name filter; `cargo test -p async-opcua-server --test security_tests` initially failed `encrypted_secret_decryption_failure_is_tarpitted`, then passed after the tarpit classification fix with 25 passed | Fixed tarpit classification in `async-opcua-server/src/session/negotiate.rs` so policy-protection failures preserve `BadIdentityTokenInvalid` while encrypted secret decryption failures normalize to `BadUserAccessDenied` | Meaningful security test target is green: `25 passed; 0 failed`; exact task command remains a no-op filter | Consider correcting the task/CI command to `cargo test -p async-opcua-server --test security_tests` in future specs |
| T021b | `cargo test -p async-opcua-server session::manager` passed | Worker ran the requested session-manager checkpoint | 12 matching `session::manager` unit tests passed; other test targets had 0 matching tests due to filter | None |
| T021c | `cargo test -p async-opcua --test integration_tests adversarial` passed | Worker ran the requested adversarial integration checkpoint | 11 matching tests passed; 266 filtered out | None |
| T022 | `cargo test -p async-opcua-server application_certificate_rejected_store_preserves_create_session_and_open_secure_channel_statuses` passed | Added application-certificate rejected-store public contract lock-in test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 6.1.3 and 5.7.2.2 | Existing behavior already preserves the public contract: CreateSession rejected-store application certificate returns `BadCertificateUntrusted`; OpenSecureChannel rejected-store application certificate returns `BadSecurityChecksFailed` | T023 owns user-identity X.509 rejected-store precision and audit emission |
| T023 | `cargo test -p async-opcua-server --test security_tests x509_user_token_rejected_store_certificate_is_untrusted_and_audited -- --exact --nocapture` passed | Added user-identity X.509 rejected-store audit/status lock-in test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 6.1.3, 5.7.3.3 and OPC-10000-5 6.4.12/6.4.16 | Existing behavior already maps rejected-store X.509 user certificates to `BadCertificateUntrusted` and emits the expected certificate audit event | T036 still owns any production split/audit refinements needed after broader certificate status tests land |
| T024 | `cargo test -p async-opcua-crypto issuer_sha1_signature_is_policy_check_failed` failed as expected because the chain was accepted with `Ok([])` | Added red SHA-1 issuer-signature chain test in `async-opcua-crypto/src/tests/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 Table 100 Security Policy Check | Expected-red command recorded; T037 must reject disallowed issuer signature algorithms with `BadCertificatePolicyCheckFailed` or the accepted precise certificate status | Production behavior not changed in this test task |
| T025 | `cargo test -p async-opcua-crypto tests::cert_chain::crl_sha1_signature_is_policy_check_failed -- --exact` failed as expected because the validator returned `Ok([])` | Added red RSA-SHA1 CRL signature test in `async-opcua-crypto/src/tests/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 Table 100 Security Policy Check plus revocation checks | Expected-red command recorded; T038 must reject disallowed revocation/CRL signature algorithms with `BadCertificatePolicyCheckFailed` or the accepted precise certificate status | Production behavior not changed in this test task |
| T026 | `cargo test -p async-opcua-crypto tests::cert_chain::ca_path_length_constraint_violation_is_issuer_use_not_allowed -- --exact` failed as expected because the validator returned `Ok([])` | Added red pathLenConstraint=0 subordinate-CA chain test in `async-opcua-crypto/src/tests/cert_chain.rs`; MCP refs OPC-10000-4 6.1.3 Table 100 Build Certificate Chain and Certificate Usage | Expected-red command recorded; T039 must reject CA path-length constraint violations with `BadCertificateIssuerUseNotAllowed` or the accepted precise certificate status | Production behavior not changed in this test task |
| T027a | `cargo test -p async-opcua-crypto tests::cert_chain::store_missing_trusted_directory_fails_closed_even_with_trust_unknown_enabled -- --exact` failed as expected because validation returned `Ok(())` | Added red missing trusted-storage fail-closed test in `async-opcua-crypto/src/tests/cert_chain.rs`; MCP refs OPC-10000-4 6.1.3 Trust List Check and OPC-10000-4 7.38.2 `BadCertificateUntrusted` | Expected-red command recorded; T040a must fail closed when required PKI trust storage is missing, even with trust-unknown enabled | Production behavior not changed in this test task |
| T027b | `cargo test -p async-opcua-crypto tests::cert_chain::store_corrupt_trusted_certificate_file_fails_closed_with_certificate_context -- --exact` failed as expected because validation returned `Ok(())` | Added red corrupt trusted-storage fail-closed/audit-context test in `async-opcua-crypto/src/tests/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 Trust List Check and certificate audit context | Expected-red command recorded; T040b must fail closed on corrupt PKI storage and return auditable certificate context | Production behavior not changed in this test task |
| T028 | `cargo test -p async-opcua-server --test gds_pull_methods get_rejected_list_requires_authenticated_secure_channel_and_security_admin_before_registry_read -- --exact` failed as expected because rejected certificate data was returned in `Ok([...])` | Added red GetRejectedList authorization-before-read test in `async-opcua-server/tests/gds_pull_methods.rs`; MCP refs OPC-10000-12 7.8.3.2 and 7.2 | Expected-red command recorded; T041 must pass request security context into GetRejectedList and reject unauthorized callers before registry reads | Production behavior not changed in this test task |
| T029 | `cargo test -p async-opcua-server --test gds_pull_methods update_certificate_requires_encrypted_secure_channel_and_security_admin_before_recording_material` failed as expected because current behavior returned `Ok([Boolean(true)])` | Added red UpdateCertificate authorization-before-mutation test in `async-opcua-server/tests/gds_pull_methods.rs`; MCP refs OPC-10000-12 7.10.5, 7.2, and 7.4 | Expected-red command recorded; T042 must enforce encrypted SecureChannel plus SecurityAdmin before recording certificate material | Production behavior not changed in this test task |
| T030 | `cargo test -p async-opcua-server gds::push_methods::tests::create_signing_request_requires_encrypted_secure_channel_and_security_admin_before_recording_request_state -- --exact` failed as expected because current behavior returned a CSR and appended request state | Added red CreateSigningRequest authorization-before-state-generation test in `async-opcua-server/src/gds/push_methods.rs`; MCP refs OPC-10000-12 7.10.10, 7.2, and 7.4 | Expected-red command recorded; T043 must enforce encrypted SecureChannel plus SecurityAdmin before generating signing request state | Production behavior not changed in this test task |
| T031 | `cargo test -p async-opcua-server --test gds_pull_methods finish_signing_request_helper_requires_encrypted_channel_and_security_admin_before_returning_material -- --exact` failed as expected because the helper leaked two certificate-material outputs | Added red implementation-specific FinishSigningRequest helper authorization test in `async-opcua-server/tests/gds_pull_methods.rs`; MCP refs OPC-10000-12 7.4 and 7.10.5 | Expected-red command recorded; T044 must enforce inherited Push Management encrypted SecureChannel plus SecurityAdmin guard before returning certificate material; the helper is not labeled as a standard OPC UA method | Production behavior not changed in this test task |
| T032a | `cargo test -p async-opcua-server --test gds_pull_methods update_certificate_rejects_malformed_der_certificate_without_mutating_registry -- --exact` failed as expected because malformed bytes were recorded in `updated_certificates` | Added red malformed DER certificate input/no-mutation test in `async-opcua-server/tests/gds_pull_methods.rs`; MCP ref OPC-10000-12 7.10.5 `UpdateCertificate` and `BadCertificateInvalid` | Expected-red command recorded; T045a must validate DER certificate input before registry mutation | Production behavior not changed in this test task |
| T032b | `cargo test -p async-opcua-server --test gds_pull_methods update_certificate_rejects_malformed_private_key_without_mutating_registry -- --exact` failed as expected because malformed private-key bytes were recorded in `updated_certificates` | Added valid DER certificate helper plus red malformed private-key/no-mutation test in `async-opcua-server/tests/gds_pull_methods.rs`; MCP ref OPC-10000-12 7.10.5 `UpdateCertificate` private-key inputs and `BadNotSupported` mapping | Expected-red command recorded; T045b must validate private-key input before registry mutation | Production behavior not changed in this test task |
| T033 | `cargo test -p async-opcua-server --test gds_cache failed_private_key_replacement_preserves_previous_cached_credential_pair -- --exact` failed as expected because the replacement certificate was written while the previous private key remained | Added red GDS cache replacement atomicity test in `async-opcua-server/tests/gds_cache.rs`; MCP refs OPC-10000-12 7.4 and 7.10.5 | Expected-red command recorded; T046 must write cached certificate/private-key/trust-list material atomically so failed replacement preserves the previous credential pair | Production behavior not changed in this test task |
| T034a | `cargo test -p async-opcua --test integration_tests open_secure_channel_invalid_certificate_audit_uses_certificate_source_name` failed as expected because `AuditCertificateInvalidEventType` was not delivered after an OpenSecureChannel certificate rejection | Added red OpenSecureChannel certificate audit source-name integration test in `async-opcua/tests/integration/adversarial.rs`; MCP refs OPC-10000-5 6.4.15 and OPC-10000-4 6.1.3 | Expected-red command recorded; T047a must emit/normalize OpenSecureChannel certificate audit events with source name `Security/Certificate` | Production behavior not changed in this test task |
| T034b | `cargo test -p async-opcua --test integration_tests create_session_with_suppressed_client_certificate_finding_emits_success_audit_event` failed as expected because `AuditCertificateExpiredEventType` success audit was not delivered | Added red suppressed certificate-validation success audit integration test in `async-opcua/tests/integration/adversarial.rs`; MCP refs OPC-10000-4 5.7.2.2, 6.1.3 and OPC-10000-5 6.4.15/6.4.12 | Expected-red command recorded; T047b must emit auditable certificate context with `Status=true` for suppressed findings that still allow success | Production behavior not changed in this test task |
| T035 | `cargo test -p async-opcua-server application_certificate_rejected_store_preserves_create_session_and_open_secure_channel_statuses`, `cargo test -p async-opcua-server --test security_tests x509_user_token_rejected_store_certificate_is_untrusted_and_audited -- --exact --nocapture`, `cargo test -p async-opcua-crypto store_rejected_directory_hit_reports_certificate_untrusted`, and `cargo test -p async-opcua-crypto store_user_identity_short_key_reports_policy_check_failed` passed | Added private application-vs-user-identity certificate status mapper in `async-opcua-crypto/src/certificate_store.rs`; MCP ref OPC-10000-4 6.1.3 | Application-certificate public behavior remains locked while user-identity certificate validation keeps precise certificate statuses for rejected-store and policy-check cases | T040a/T040b storage fail-closed tests remain expected-red until storage handling is implemented |
| T036 | `cargo test -p async-opcua-server --test security_tests x509_user_token_rejected_store_certificate_is_untrusted_and_audited -- --exact --nocapture`, `cargo test -p async-opcua-server --test security_tests x509_user_token_untrusted_configured_thumbprint_is_rejected -- --exact --nocapture`, `cargo test -p async-opcua-server application_certificate_rejected_store_preserves_create_session_and_open_secure_channel_statuses`, `rustfmt --edition 2021 --check async-opcua-server/src/session/audit.rs async-opcua-server/src/authenticator.rs`, and `git diff --check -- async-opcua-server/src/session/audit.rs async-opcua-server/src/authenticator.rs` passed | No code change needed; current audit/authenticator path already emits user-identity X.509 rejected-store status and audit context; MCP refs OPC-10000-4 6.1.3 and OPC-10000-5 6.4.12/6.4.16 | T023 remains green after the T035 application-vs-user certificate status split | None |
| T037 | `cargo test -p async-opcua-crypto tests::cert_chain::issuer_sha1_signature_is_policy_check_failed -- --exact --nocapture`, `cargo test -p async-opcua-crypto tests::cert_chain::leaf_key_too_short_is_policy_check_failed -- --exact --nocapture`, `cargo test -p async-opcua-crypto tests::cert_chain::suppressed_security_policy_passes_with_finding -- --exact --nocapture`, `cargo test -p async-opcua-crypto tests::cert_chain::forged_leaf_signature_is_invalid -- --exact --nocapture`, `cargo test -p async-opcua-crypto tests::cert_chain::store_user_identity_short_key_reports_policy_check_failed -- --exact --nocapture`, `rustfmt --edition 2021 --check async-opcua-crypto/src/cert_chain.rs`, and `git diff --check -- async-opcua-crypto/src/cert_chain.rs` passed | Added chain-wide certificate signature algorithm validation in `async-opcua-crypto/src/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 Table 100 Security Policy Check | Issuer certificate signatures now fail through the existing suppressible security-policy path without changing leaf key-length behavior or `SecurityPolicy::None` | T038 revocation signature red test remains expected-red until CRL/revocation algorithm validation is implemented |
| T038 | Worker verification passed `cargo test -p async-opcua-crypto tests::cert_chain::crl_sha1_signature_is_policy_check_failed -- --exact --nocapture`, `tests::cert_chain::issuer_sha1_signature_is_policy_check_failed`, CRL/revocation/suppression/policy/OCSP filtered tests, `rustfmt --edition 2021 --check async-opcua-crypto/src/cert_chain.rs`, and `git diff --check -- async-opcua-crypto/src/cert_chain.rs`; main confirmation `cargo test -p async-opcua-crypto sha1_signature_is_policy_check_failed -- --nocapture` and rustfmt check passed | Added CRL/OCSP revocation signature algorithm validation for issuer-authenticated revocation material in `async-opcua-crypto/src/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 Security Policy Check / Revocation Check | CRLs and OCSP responses signed with algorithms disallowed by the active SecurityPolicy now fail through the existing suppressible security-policy path after relevance and issuer signature verification | T039/T040a/T040b remain expected-red until path-length and storage fail-closed work is implemented |
| T039 | Worker verification passed `ca_path_length_constraint_violation_is_issuer_use_not_allowed`, issuer/CRL SHA-1 policy tests, nearby issuer/usage tests, suppression tests, `rustfmt --edition 2021 --check async-opcua-crypto/src/cert_chain.rs`, and `git diff --check -- async-opcua-crypto/src/cert_chain.rs`; main confirmation passed `cargo test -p async-opcua-crypto tests::cert_chain::ca_path_length_constraint_violation_is_issuer_use_not_allowed -- --exact --nocapture` and `cargo test -p async-opcua-crypto sha1_signature_is_policy_check_failed -- --nocapture` | Added CA BasicConstraints path-length enforcement in `async-opcua-crypto/src/cert_chain.rs`; MCP ref OPC-10000-4 6.1.3 issuer-use validation | Issuer certificates now reject chains whose subordinate CA count exceeds `path_len_constraint`, using the existing certificate-usage suppression/status path | T040a/T040b storage fail-closed tests remain expected-red until certificate store handling is implemented; one discarded local Cargo command used two filters and exited with CLI usage error |
| T040a | Worker verification passed `store_missing_trusted_directory_fails_closed_even_with_trust_unknown_enabled`, `store_rejected_directory_hit_reports_certificate_untrusted`, `store_user_identity_short_key_reports_policy_check_failed`, `store_rejects_untrusted_leaf_and_files_it_under_rejected`, `store_user_identity_validation_returns_suppressed_findings`, `rustfmt --edition 2021 --check async-opcua-crypto/src/certificate_store.rs`, and `git diff --check -- async-opcua-crypto/src/certificate_store.rs`; main confirmation passed `cargo test -p async-opcua-crypto tests::cert_chain::store_missing_trusted_directory_fails_closed_even_with_trust_unknown_enabled -- --exact --nocapture` plus format/diff checks | Added trusted-directory availability fail-closed guard in `async-opcua-crypto/src/certificate_store.rs`; MCP ref OPC-10000-4 6.1.3 trust-list validation | Missing/unreadable `trusted/` storage now returns `BadCertificateUntrusted` with certificate filename/thumbprint context before `trust_unknown_certs` can auto-store the presented certificate, preserving T035 status mapping | T040b corrupt trusted certificate file handling remains expected-red until corrupt-storage validation is implemented |
| T040b | Worker verification passed the required corrupt/missing/rejected/user-identity exact tests, `tests::cert_chain::store_` filter, `rustfmt --edition 2021 --check async-opcua-crypto/src/certificate_store.rs`, and `git diff --check -- async-opcua-crypto/src/certificate_store.rs`; main confirmation passed `cargo test -p async-opcua-crypto tests::cert_chain::store_corrupt_trusted_certificate_file_fails_closed_with_certificate_context -- --exact --nocapture` and `store_missing_trusted_directory_fails_closed_even_with_trust_unknown_enabled` | Added strict trusted-certificate directory loading for validation in `async-opcua-crypto/src/certificate_store.rs`; MCP ref OPC-10000-4 6.1.3 trust-list validation | Corrupt trusted certificate files now return `BadCertificateUntrusted` with certificate filename/thumbprint context before `trust_unknown_certs` can auto-store the presented certificate, preserving T035/T040a behavior | None for the requested corrupt trusted-certificate path |
| T041 | Initial worker passed the no-context denial test but exposed a stale no-context happy-path test; repair worker passed `cargo test -p async-opcua-server --test gds_pull_methods get_rejected_list -- --nocapture`, `get_rejected_list_requires_authenticated_secure_channel_and_security_admin_before_registry_read -- --exact --nocapture`, rustfmt, and diff checks; main confirmation passed `cargo test -p async-opcua-server --test gds_pull_methods get_rejected_list -- --nocapture` | Added context-aware GetRejectedList handling in `async-opcua-server/src/gds/pull_methods.rs` and updated the happy-path test to use a SecurityAdmin request context; MCP ref OPC-10000-12 7.8.3.2 | GetRejectedList now rejects no-context/`None` security before registry reads and uses the node-manager request context for authorized calls; existing callback plumbing in `node_manager/method.rs` already passed `RequestContext` | Full `gds_pull_methods` remains pending until T042-T045 are implemented |
| T042 | Worker verification passed `update_certificate_requires_encrypted_secure_channel_and_security_admin_before_recording_material -- --exact --nocapture`, `update_certificate_records_certificate_material_and_requires_apply_changes -- --exact --nocapture`, `update_certificate_rejects_empty_certificate -- --exact --nocapture`, rustfmt, and diff checks; main confirmation reran all three focused tests successfully | Added context-aware UpdateCertificate handling in `async-opcua-server/src/gds/pull_methods.rs` and updated tests to use authorized `SignAndEncrypt` SecurityAdmin request contexts; MCP ref OPC-10000-12 7.10.5 | UpdateCertificate now rejects no-context, signed-only, and non-SecurityAdmin callers before recording certificate material while authorized calls keep existing behavior | T044/T045 remain pending for FinishSigningRequest authorization and DER/private-key validation |
| T043 | Worker verification passed `gds::push_methods::tests::create_signing_request_requires_encrypted_secure_channel_and_security_admin_before_recording_request_state -- --exact --nocapture`, `gds::push_methods::tests::create_signing_request -- --nocapture`, missing-argument test, rustfmt, and diff checks; main confirmation reran the required module-qualified denial test and the `create_signing_request` filter successfully | Added context-aware CreateSigningRequest handling in `async-opcua-server/src/gds/push_methods.rs`; MCP ref OPC-10000-12 7.10.10 | CreateSigningRequest now rejects no-context, signed-only, and non-SecurityAdmin callers before request-state mutation while authorized calls preserve CSR behavior | The unqualified exact Cargo filter matched 0 tests; use the module-qualified test path for exact execution |
| T044 | Worker verification passed `finish_signing_request_helper_requires_encrypted_channel_and_security_admin_before_returning_material -- --exact --nocapture`, `finish_signing_request_returns_completed_certificate_material -- --exact --nocapture`, `update_certificate_requires_encrypted_secure_channel_and_security_admin_before_recording_material -- --exact --nocapture`, rustfmt, and diff checks; main confirmation reran the two FinishSigningRequest focused tests successfully | Added context-aware FinishSigningRequest helper authorization in `async-opcua-server/src/gds/pull_methods.rs`; MCP refs OPC-10000-12 7.4 and 7.10.5 | The implementation-specific helper now requires `SignAndEncrypt` plus SecurityAdmin before returning certificate/private-key material and keeps the existing method-id contract without adding a standard method | T045 DER/private-key validation and T046 cache atomicity remain pending |
| T045a | Worker verification passed `update_certificate_rejects_malformed_der_certificate_without_mutating_registry -- --exact --nocapture`, `update_certificate_records_certificate_material_and_requires_apply_changes -- --exact --nocapture`, `update_certificate_requires_encrypted_secure_channel_and_security_admin_before_recording_material -- --exact --nocapture`, rustfmt, diff checks, and `update_certificate_rejects_empty_certificate`; main confirmation reran malformed-DER and authorized UpdateCertificate focused tests successfully | Added UpdateCertificate DER certificate parsing before registry mutation in `async-opcua-server/src/gds/pull_methods.rs`; MCP ref OPC-10000-12 7.10.5 | Malformed certificate bytes now return `BadCertificateInvalid` without mutating recorded certificate material while authorization remains first | T045b private-key validation remains pending |
| T045b | Worker verification passed malformed private-key, malformed DER, authorized UpdateCertificate, authorization-ordering tests, rustfmt, diff checks, and full `cargo test -p async-opcua-server --test gds_pull_methods -- --nocapture`; main confirmation reran malformed private-key and full `gds_pull_methods` successfully | Added UpdateCertificate private-key validation before registry mutation in `async-opcua-server/src/gds/pull_methods.rs`; MCP ref OPC-10000-12 7.10.5 | Malformed or unsupported private-key input now returns `BadNotSupported` without mutating material, while empty key plus empty/null format remains allowed and DER validation/authorization ordering are preserved | `PFX` remains unsupported because no PKCS#12 parser exists in the current crypto layer |
| T046 | Worker verification passed `failed_private_key_replacement_preserves_previous_cached_credential_pair -- --exact --nocapture`, full `cargo test -p async-opcua-server --test gds_cache -- --nocapture`, rustfmt, and diff checks; main confirmation reran the focused failure test and full `gds_cache` successfully | Added same-directory staged files, sync, atomic rename, and rollback for GDS cached credential replacement in `async-opcua-server/src/gds/cache.rs`; MCP refs OPC-10000-12 7.4 and 7.10.5 | Simulated private-key replacement failure now preserves the previous certificate/private-key/trust-list set instead of leaving mixed material | Process/host crash during the multi-file replacement window is still not fully transactional with `std::fs` |
| T047a | Worker verification passed the module-qualified OpenSecureChannel certificate-audit source-name regression, adjacent hard X.509 certificate audit failure test, rustfmt, and diff checks; main confirmation passed `cargo test -p async-opcua --test integration_tests integration::adversarial::open_secure_channel_invalid_certificate_audit_uses_certificate_source_name -- --exact --nocapture` | Added OpenSecureChannel certificate audit dispatch in `async-opcua-server/src/session/audit.rs` and emitted it from `async-opcua-server/src/session/controller.rs`; MCP refs OPC-10000-5 6.4.15 and OPC-10000-4 6.1.3 | OpenSecureChannel certificate validation failures now emit `AuditCertificateInvalidEventType` with source name `Security/Certificate` before returning `BadSecurityChecksFailed` | T047b suppressed-success audit behavior remains pending; unqualified exact Cargo filter matched 0 tests |
| T047b | Worker verification passed module-qualified `create_session_with_suppressed_client_certificate_finding_emits_success_audit_event`, `open_secure_channel_invalid_certificate_audit_uses_certificate_source_name`, hard X.509 failure audit, suppressed X.509 finding audit, rustfmt, and diff checks; main confirmation reran the suppressed-success and OpenSecureChannel audit regressions successfully | Added suppressed certificate-validation success audit dispatch in `async-opcua-server/src/session/audit.rs` and emitted it from `async-opcua-server/src/session/controller.rs`; MCP refs OPC-10000-4 5.7.2.2 and 6.1.3 plus OPC-10000-5 6.4.15 | Successful CreateSession with the selected suppressed application-certificate validity finding now emits `AuditCertificateExpiredEventType` with source `Security/Certificate` and `Status=true`; T047a failure audit remains green | Detection is scoped to the configured `check_cert_time=false` application-certificate validity suppression because the CreateSession controller path still does not receive structured suppressed findings from application-certificate validation |
| T048a | N/A checkpoint verification | Worker ran `cargo test -p async-opcua-crypto cert_chain -- --nocapture`, `rustfmt --edition 2021 --check async-opcua-crypto/src/cert_chain.rs async-opcua-crypto/src/certificate_store.rs async-opcua-crypto/src/tests/cert_chain.rs`, and `git diff --check -- async-opcua-crypto/src/cert_chain.rs async-opcua-crypto/src/certificate_store.rs async-opcua-crypto/src/tests/cert_chain.rs` | Crypto cert-chain checkpoint passed: 46 passed, 0 failed, 92 filtered; rustfmt and diff checks passed | None |
| T048b | N/A checkpoint verification | Worker ran `cargo test -p async-opcua-server --test gds_pull_methods -- --nocapture`, rustfmt check for `async-opcua-server/src/gds/pull_methods.rs async-opcua-server/tests/gds_pull_methods.rs`, and matching `git diff --check` | GDS pull-method checkpoint passed: 10 passed, 0 failed, 0 filtered; rustfmt and diff checks passed | None |
| T048c | N/A checkpoint verification | Worker ran `cargo test -p async-opcua-server --test gds_integration -- --nocapture`, rustfmt check for GDS pull/push/cache files plus `async-opcua-server/tests/gds_integration.rs`, and matching `git diff --check` | GDS integration checkpoint passed: `test_zero_downtime_certificate_rotation` passed; 1 passed, 0 failed; rustfmt and diff checks passed | None |
| T048d | N/A checkpoint verification | Worker ran `cargo test -p async-opcua --test integration_tests adversarial -- --nocapture`, rustfmt check for session audit/controller plus adversarial integration test, and matching `git diff --check` | Adversarial checkpoint passed: 13 passed, 0 failed, 266 filtered; rustfmt and diff checks passed; expected negative-path ERROR logs were observed | None |
| T049 | `cargo test -p async-opcua-core oversized -- --nocapture` failed as expected: new `decode_rejects_oversized_declared_message_size_before_payload_allocation` got `Ok(None)` instead of `BadTcpMessageTooLarge`, proving the decoder still waits for an impossible declared payload | Added red oversized declared message-size test in `async-opcua-core/src/comms/tcp_codec.rs`; MCP refs OPC-10000-6 7.1.2.2 and 7.1.5 | Expected-red command recorded; production fix must introduce a real negotiated/hard receive bound before waiting or allocating for the declared payload | The test targets the `max_message_size: 0` unbounded path; rustfmt and diff checks passed |
| T050 | `cargo test -p async-opcua-core pre_hello -- --nocapture` failed as expected: new `pre_hello_ack_and_err_declared_message_sizes_are_bounded_before_negotiation` got `Ok(None)` instead of `BadTcpMessageTooLarge`, proving ACK/ERR oversized headers are not bounded before negotiation; `cargo test -p async-opcua-core oversized -- --nocapture` also remained expected-red due T049 | Added red pre-Hello ACK/ERR declared-size bound test in `async-opcua-core/src/comms/tcp_codec.rs`; MCP ref OPC-10000-6 7.1.2.2 | Expected-red command recorded; production fix must reject oversized ACK/ERR headers before waiting for the declared payload | Test-only change; rustfmt and diff checks passed |
| T051 | `cargo test -p async-opcua-core ecc_policy_allows_1024_hello_ack_buffer_sizes_without_weakening_default_minimum -- --nocapture` failed as expected: `SecurityPolicy::EccNistP256` HEL with 1024-byte buffers is rejected by the current 8192-byte floor | Added expected-red ECC policy-aware Hello/ACK buffer-size test in `async-opcua-core/src/comms/tcp_types.rs`; MCP refs OPC-10000-6 7.1.2.3 and 7.1.2.4 | Expected-red command recorded; production fix must make buffer-size validation policy-aware without weakening default/non-ECC minimums | ACK assertion is behind the failing HEL assertion until production validation supports ECC intent; rustfmt and diff checks passed |
| T052 | `cargo test -p async-opcua-core asymmetric_header_decode_rejects_security_policy_uri_longer_than_255_bytes -- --nocapture` failed as expected: `AsymmetricSecurityHeader::decode` accepted a 256-byte `SecurityPolicyUri` instead of returning `BadEncodingLimitsExceeded` | Added red asymmetric SecurityPolicyUri length decode test in `async-opcua-core/src/comms/security_header.rs`; MCP ref OPC-10000-6 6.7.2.3 | Expected-red command recorded; production fix must reject `SecurityPolicyUriLength > 255` during asymmetric header decoding/validation | Test-only change; rustfmt and diff checks passed |
| T053 | `cargo test -p async-opcua-core unknown_service -- --nocapture` failed as expected: current `RequestMessage::decode_by_object_id` returns `BadDecodingError` for an unsupported request object id instead of `BadServiceUnsupported` | Added red unknown-service-id request decode test in `async-opcua-core/src/messages/request.rs`; MCP refs OPC-10000-4 5.3 and 7.34 | Expected-red command recorded; production fix must surface `BadServiceUnsupported` while preserving request id/handle context for a reusable-channel ServiceFault | Test targets the `request.rs` unsupported object-id path; truly unknown numeric NodeId rejection earlier in chunk decoding may need separate T079 coverage; rustfmt and diff checks passed |
| T054 | `cargo test -p async-opcua-server --test security_tests max_response -- --nocapture` failed as expected: new `max_response_message_size_rejects_serialized_read_response_body_above_client_limit` expected `BadResponseTooLarge`, but the oversized `ReadResponse` succeeded (`got None, success=true`) | Added red maxResponseMessageSize response-size test in `async-opcua-server/tests/security_tests.rs`; MCP refs OPC-10000-4 5.7.2.2 and 5.3 | Expected-red command recorded; production fix must enforce client response-size limits before sending serialized response bodies | Test-only change; rustfmt and diff checks passed |
| T055 | `cargo test -p async-opcua-server --test service_diagnostics return_diagnostics -- --nocapture` failed as expected: `serviceDiagnostics must be populated when service-level returnDiagnostics are requested` | Added red returnDiagnostics ServiceFault test in `async-opcua-server/tests/service_diagnostics.rs`; MCP refs OPC-10000-4 7.32 and 7.34 | Expected-red command recorded; production fix must populate `ResponseHeader.serviceDiagnostics` and `stringTable` only when requested | Test-only change; rustfmt and whitespace checks passed |
| T056 | `cargo test -p async-opcua-types monitoring_mode_raw_decode_preserves_invalid_value_for_service_status -- --nocapture` failed as expected: invalid raw value `3` decoded as `BadDecodingError` before service code can return `BadMonitoringModeInvalid` | Added red MonitoringMode raw-decode preservation test in `async-opcua-types/src/generated/types/enums.rs`; MCP ref OPC-10000-4 5.13.4.3 Table 70 | Expected-red command recorded; production fix must preserve invalid MonitoringMode values long enough for SetMonitoringMode to return `BadMonitoringModeInvalid` | Test-only change; rustfmt and diff checks passed |
| T057 | `cargo test -p async-opcua --test integration_tests invalid_monitoring_mode -- --nocapture` failed as expected: invalid raw `MonitoringMode` value `3` is rejected during enum decode with `BadDecodingError` before SetMonitoringMode can return `BadMonitoringModeInvalid` | Added red SetMonitoringMode invalid monitoring-mode integration test in `async-opcua/tests/integration/subscriptions.rs`; MCP ref OPC-10000-4 5.13.4.3 Table 70 | Expected-red command recorded; production fix must let invalid MonitoringMode reach service validation and return `BadMonitoringModeInvalid` | Test-only change; rustfmt and diff checks passed |
| T058a | `cargo test -p async-opcua-types json_int64_encodes_and_decodes_decimal_string -- --nocapture` failed as expected: Int64 string encoding/decoding passed, but JSON-number Int64 input was still accepted | Added red Int64 JSON decimal-string conformance test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.3 | Expected-red command recorded; production fix must reject JSON-number Int64 input while preserving decimal-string encode/decode | Test-only change; rustfmt and diff checks passed |
| T058b | `cargo test -p async-opcua-types json_uint64 -- --nocapture` failed as expected: UInt64 string encoding/decoding passed, but JSON-number UInt64 input was still accepted | Added red UInt64 JSON decimal-string conformance test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.3 | Expected-red command recorded; production fix must reject JSON-number UInt64 input while preserving decimal-string encode/decode | Test-only change; rustfmt and diff checks passed |
| T059a | `cargo test -p async-opcua-types json_nodeid_uses_opc_ua_1_05_string_form -- --nocapture` failed as expected: `NodeId::new(2, \"Pump/Line1\")` serialized as the legacy object instead of `\"ns=2;s=Pump/Line1\"` | Added red NodeId OPC UA 1.05 JSON string-form test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.10 | Expected-red command recorded; production fix must encode/decode NodeId JSON using the Part 6 §5.1.12 string form | Test-only change; rustfmt and diff checks passed |
| T059b | `cargo test -p async-opcua-types json_expanded_nodeid_uses_opc_ua_1_05_string_form -- --nocapture` failed as expected: ExpandedNodeId with `svr=7;nsu=urn:example:expanded;i=321` serialized as the legacy object instead of the 1.05 string form | Added red ExpandedNodeId OPC UA 1.05 JSON string-form test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.11 | Expected-red command recorded; production fix must encode/decode ExpandedNodeId JSON using the Part 6 §5.1.12 string form | Test-only change; rustfmt and diff checks passed |
| T060 | `cargo test -p async-opcua-types json_variant_uses_uatype_and_value_fields -- --nocapture` failed as expected: `Variant::Double(1.25)` serialized with legacy `Body` instead of `Value` | Added red Variant JSON `UaType`/`Value` field test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.17 | Expected-red command recorded; production fix must switch Variant JSON to `UaType`/`Value` and decode the same shape | Existing adjacent test still locks legacy `Body` behavior and will need revision with T086; rustfmt and diff checks passed |
| T061a | `cargo test -p async-opcua-types extension_object_uabody -- --nocapture` failed as expected: valid `UaTypeId` with `UaBody: null` returned `BadDecodingError` instead of decoding to a null ExtensionObject body | Added red ExtensionObject `UaBody`/null-body JSON test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.16 | Expected-red command recorded; production fix must accept the Part 6 `UaBody` shape and decode null bodies correctly while rejecting legacy `Body` | Test-only change; rustfmt and diff checks passed |
| T061b | `cargo test -p async-opcua-types extension_object_duplicate -- --nocapture` passed in the current tree: raw JSON with duplicate `UaTypeId` fields is already rejected | Added lock-in ExtensionObject duplicate JSON field-name test in `async-opcua-types/src/tests/json.rs`; MCP ref OPC-10000-6 5.4.2.16 | Already passing, so no production fix is required for this duplicate-field path | Not expected-red because current decoder already rejects duplicates when raw JSON text avoids `serde_json::Value` key collapse; rustfmt and diff checks passed |
| T062a | `cargo test -p async-opcua-pubsub --test pubsub_tests invalid_flag -- --nocapture` failed as expected: invalid UADP `ExtendedFlags1` combination was not rejected before subscriber state changed | Added red invalid UADP flag/state-preservation test in `async-opcua-pubsub/tests/pubsub_tests.rs`; MCP ref OPC-10000-14 7.2.4.4.2 | Expected-red command recorded; production fix must reject invalid UADP headers before subscriber state updates | Covers PicoSeconds-without-Timestamp invalid flag combination, not every reserved UADP flag; rustfmt and diff checks passed |
| T062b | `cargo test -p async-opcua-pubsub --test pubsub_tests field_count -- --nocapture` passed in the current tree: `field_count = 65535` is rejected with `BadDecodingError` and subscriber state remains unchanged | Existing `uadp_dataset_message_rejects_field_count_above_decoding_limit` in `async-opcua-pubsub/tests/pubsub_tests.rs` already covers the overflow path; MCP refs OPC-10000-14 7.2.4.4.2 and 7.2.4.5.5 | Already passing, so no production fix is required for this field-count overflow path | First worker's zero DataSetMessage count test was removed as out-of-scope; rustfmt and diff checks passed |
| T063a | `cargo test -p async-opcua-pubsub --test message_security_tests trailing -- --nocapture` failed as expected: secured payload with authenticated trailing plaintext decoded successfully instead of returning `BadSecurityChecksFailed` | Added red secured UADP trailing-payload/replay-state test in `async-opcua-pubsub/tests/message_security_tests.rs`; MCP refs OPC-10000-14 7.2.4.4.2 and 7.2.4.4.3 | Expected-red command recorded; production fix must reject trailing secured payload bytes before advancing replay state | Replay non-advancement assertion is present but blocked until rejection is implemented; rustfmt and diff checks passed |
| T063b | `cargo test -p async-opcua-pubsub --test message_security_tests oversized -- --nocapture` passed in the current tree: over-limit secured payload returns `BadSecurityChecksFailed` and the same valid message is accepted afterward, proving replay state did not advance | Added lock-in oversized secured UADP payload/replay-state test in `async-opcua-pubsub/tests/message_security_tests.rs`; MCP refs OPC-10000-14 7.2.4.4.3.2 and 7.2.4.4.2 | Already passing, so no production fix is required for this configured payload-limit path | Limit is enforced through `DecodingOptions::max_secured_payload_len`, not a protocol-maximum-sized fixture; rustfmt and diff checks passed |
| T064 | `cargo test -p async-opcua-server --test security_tests non_current_starting_token -- --nocapture` failed as expected: retained historical keys can be modeled only by making token `5` the current first token, so `StartingTokenId=0` incorrectly returns first token `5` instead of current token `7` | Added red SKS historical `StartingTokenId` range test in `async-opcua-server/tests/security_tests.rs`; MCP ref OPC-10000-14 8.3.2 | Expected-red command recorded; production fix must distinguish current token id from retained historical range and preserve exact current/future token lookup | Test also asserts explicit token `5` returns historical keys and token `7` returns current/next keys; rustfmt and diff checks passed |
| T065a | `cargo test -p async-opcua-xml malformed_xml_import_exposes_parse_failure -- --nocapture` passed in the current tree: malformed imported BSD XML returns `XmlErrorInner::Xml(_)` | Added lock-in malformed local XML import test in `async-opcua-xml/src/parser.rs`; MCP ref OPC-10000-6 7.4.3 | Already passing, so no production fix is required for this malformed import path | Covers local BSD import well-formedness failure, not malformed XSD, remote imports, or oversized XML; rustfmt and diff checks passed |
| T065b | `cargo test -p async-opcua-xml oversized_xml_import_is_rejected_before_parsing -- --nocapture` failed as expected: a 1,048,577-byte local XML import parsed without a size-bound error | Added red oversized local XML import test in `async-opcua-xml/src/parser.rs`; MCP ref OPC-10000-6 7.4.3 | Expected-red command recorded; production fix must reject oversized local XML imports with an explicit byte-count error before parsing | 1 MiB threshold is a feature hardening contract, not a numeric OPC UA limit; rustfmt and diff checks passed |
| T066 | `cargo test -p async-opcua-history-sqlite --test history_events corrupt -- --nocapture` failed as expected: corrupt event row returns `BadInternalError` instead of `BadDataLost` while valid rows remain present/readable | Added red corrupt SQLite event-history row test in `async-opcua-history-sqlite/tests/history_events.rs`; MCP refs OPC-10000-4 5.11.3 and OPC-10000-11 6.2.2 | Expected-red command recorded; production fix must map corrupt stored event/history decode failures to explicit HistoryRead data-loss errors without deleting valid rows | Covers corrupt `historical_events.field_blob`, not raw `historical_data.value_blob`; rustfmt and diff checks passed |
| T067a | `cargo test -p async-opcua-server duplicate_browse_name_returns_operation_level_bad_browse_name_duplicated -- --nocapture` passed in the current tree: duplicate BrowseName returns operation-level `BadBrowseNameDuplicated` and does not insert the node | Added lock-in AddNodes duplicate BrowseName test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP ref OPC-10000-4 5.8.2.4 | Already passing, so no production fix is required for this memory-manager path | Test exercises `InMemoryNodeManager::add_nodes`/`NodeMutator`, not the full session service response path; rustfmt and diff checks passed |
| T067b | `cargo test -p async-opcua-server type_definition_invalid -- --nocapture` failed as expected: invalid/missing typeDefinition returned `Good` instead of operation-level `BadTypeDefinitionInvalid` | Added red AddNodes invalid typeDefinition test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP ref OPC-10000-4 5.8.2.4 | Expected-red command recorded; production fix must validate typeDefinition and leave the node uninserted on failure | Test exercises `InMemoryNodeManager::add_nodes`/`NodeMutator`, not the full session service response path; rustfmt and diff checks passed |
| T067c | `cargo test -p async-opcua-server node_attributes_invalid -- --nocapture` failed as expected: Object AddNodes with incompatible `AttributesMask::VALUE` returned `Good` instead of operation-level `BadNodeAttributesInvalid` | Added red AddNodes invalid attributes test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP ref OPC-10000-4 5.8.2.4 | Expected-red command recorded; production fix must reject node attributes invalid for the node class and leave the node uninserted | Test exercises `InMemoryNodeManager::add_nodes`/`NodeMutator`, not the full session service response path; rustfmt and diff checks passed |
| T068a | `cargo test -p async-opcua-server abstract_reference -- --nocapture` passed in the current tree: abstract `References` type returns operation-level `BadReferenceTypeIdInvalid` and does not insert the reference | Added lock-in AddReferences abstract reference type test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP ref OPC-10000-4 5.8.3.4 | Already passing, so no production fix is required for this memory-manager metadata path | Test uses explicit fixture metadata marking `i=31` abstract; it does not prove the generated standard address space carries that metadata; rustfmt and diff checks passed |
| T068b | `cargo test -p async-opcua-server duplicate_reference_returns_operation_level_bad_duplicate_reference_not_allowed -- --nocapture` failed as expected: duplicate AddReferences returned `Good` instead of operation-level `BadDuplicateReferenceNotAllowed` | Added red AddReferences duplicate-reference test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP ref OPC-10000-4 5.8.3.4 | Expected-red command recorded; production fix must reject same source/target/reference-type/direction duplicates and preserve the existing reference | Does not cover inverse-form duplicates or subtype-equivalent reference types; rustfmt and diff checks passed |
| T068c | `cargo test -p async-opcua-server structural_reference -- --nocapture` failed as expected: `Object --HasProperty--> Object` returned `Good` instead of operation-level `BadReferenceNotAllowed` | Added red AddReferences structural-reference test in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs`; MCP refs OPC-10000-4 5.8.3.4 and OPC-10000-3 7.8 | Expected-red command recorded; production fix must reject structurally disallowed references and leave no inserted reference | Selected rule covers `HasProperty` target class, not every structural reference constraint; rustfmt and diff checks passed |
| T069 | `cargo test -p async-opcua-server cross_manager_delete_nodes_delete_target_references_removes_source_manager_reference -- --nocapture` failed as expected: DeleteNodes returned `Good`, but the source-manager reference to the deleted target remained | Added red cross-manager DeleteNodes target-reference cleanup test in `async-opcua-server/src/session/services/node_management.rs`; MCP refs OPC-10000-4 5.8.4.1 and 5.8.4.2 | Expected-red command recorded; production fix must delete references to the deleted node across node managers when `deleteTargetReferences=true` | Fixture uses two in-memory managers and direct reference inspection, not a full client Browse workflow; rustfmt and diff checks passed |
| T070a | `cargo test -p async-opcua-nodes value_rank -- --nocapture` failed as expected: Variable and VariableType with `ValueRank=2` and one `ArrayDimensions` element were accepted instead of returning `BadNodeAttributesInvalid` | Added red Variable and VariableType ValueRank/ArrayDimensions count tests in `async-opcua-nodes/src/variable.rs` and `async-opcua-nodes/src/variable_type.rs`; MCP refs OPC-10000-3 5.6.2 and 5.6.5 | Expected-red command recorded; production fix must enforce positive ValueRank dimension-count consistency through the AddNodes-style attribute path | Direct constructors/setters are not covered in this task; rustfmt and diff checks passed |
| T070b | `cargo test -p async-opcua-nodes array_dimensions -- --nocapture` failed as expected: Variable and VariableType with scalar `ValueRank=-1` and present `ArrayDimensions` were accepted instead of returning `BadNodeAttributesInvalid` | Added red Variable and VariableType scalar-rank ArrayDimensions tests in `async-opcua-nodes/src/variable.rs` and `async-opcua-nodes/src/variable_type.rs`; MCP refs OPC-10000-3 5.6.2 and 5.6.5 | Expected-red command recorded; production fix must reject ArrayDimensions for scalar/non-array ranks through the AddNodes-style attribute path | Covers scalar `ValueRank=-1`, not `ValueRank=0`; rustfmt and diff checks passed |
| T071a | `cargo test -p async-opcua --test integration_tests get_endpoints_endpoint_url -- --nocapture` failed as expected: client requested `opc.tcp://QuackServer:<port>/`, but all returned endpoint URLs used `opc.tcp://localhost:<port>/` | Added red GetEndpoints client-connect URL consistency test in `async-opcua/tests/integration/discovery.rs`; MCP ref OPC-10000-4 5.5.4.2 | Expected-red command recorded; production fix must derive returned endpoint URLs from the DiscoveryEndpoint URL used by the client where required | Redness depends on the harness connect host differing from configured host; rustfmt and diff checks passed |
| T071b | `cargo test -p async-opcua --test integration_tests find_servers_endpoint_url -- --nocapture` failed as expected: client connect URL was `opc.tcp://QuackServer:<port>/`, but FindServers returned discovery URL `opc.tcp://QuackServer:<port>` without the trailing slash | Added red FindServers discovery URL consistency test in `async-opcua/tests/integration/discovery.rs`; MCP ref OPC-10000-4 5.5.2.2 | Expected-red command recorded; production fix must normalize/return discovery URLs consistently with the client-used DiscoveryEndpoint URL | Observed mismatch is trailing slash normalization, not hostname rewriting; rustfmt and diff checks passed |
| T072a | `cargo test -p async-opcua --test integration_tests localized_text_read_locale -- --nocapture` failed as expected: session preferred `de-DE`, but Read returned `en-US` DisplayName | Added red LocalizedText session-locale Read test in `async-opcua/tests/integration/read.rs`; MCP ref OPC-10000-4 5.4 | Expected-red command recorded; production fix must preserve/select localized text variants according to session `localeIds` during Read | Failure may reflect both missing localized variant preservation and Read ignoring session locale selection; rustfmt and diff checks passed |
| T072b | `cargo test -p async-opcua --test integration_tests unsupported_special_locale -- --nocapture` failed as expected: writing LocalizedText with special locale `mul` returned `Good` instead of `BadLocaleNotSupported` | Added red unsupported special-locale LocalizedText Write test in `async-opcua/tests/integration/read.rs`; MCP refs OPC-10000-4 5.4 and 5.11.4 | Expected-red command recorded; production fix must reject unsupported/special write locales and leave the original localized value unchanged | Unchanged-value assertion is present but blocked until write rejection is implemented; rustfmt and diff checks passed |
| T073 | `cargo test -p async-opcua --test integration_tests percent_deadband_modify -- --nocapture` passed in the current tree: ModifyMonitoredItems rejects PercentDeadband without EURange and accepts it after EURange is added to the existing monitored item's node | Added lock-in PercentDeadband modify-time EURange validation test in `async-opcua/tests/integration/datachange_overflow.rs`; MCP ref OPC-10000-8 7.2 | Already passing, so no production fix is required for this modify-time EURange path | Behavioral proof only; it does not instrument internal EURange read calls; rustfmt and diff checks passed |
| T074 | `cargo test -p async-opcua --test integration_tests namespace_metadata_property_nodes_are_variables -- --nocapture` failed as expected: `NamespaceUri` metadata property read `NodeClass::Object` (`Int32(1)`) instead of `NodeClass::Variable` (`Int32(2)`) | Added red NamespaceMetadata property NodeClass test in `async-opcua/tests/integration/conformance.rs`; MCP ref OPC-10000-5 6.3.13 | Expected-red command recorded; production fix must expose NamespaceMetadata property nodes as Variables/PropertyType nodes | Test asserts five mandatory metadata properties; currently fails on the first, `NamespaceUri`; rustfmt and diff checks passed |
| T075 | T049 red evidence already recorded: oversized declared `MessageSize` returned `Ok(None)` with `max_message_size: 0` instead of `BadTcpMessageTooLarge` | Added header-only declared `MessageSize` validation in `async-opcua-core/src/comms/tcp_codec.rs` before `split_to`; MCP refs OPC-10000-6 7.1.2.2 and 7.1.5 | `cargo test -p async-opcua-core oversized -- --nocapture` passed; rustfmt and diff checks passed | `max_message_size == 0` still means unlimited for negotiated message assembly, but the TCP frame decoder now uses the crate default maximum as a hard pre-allocation guard |
| T076 | T050 red evidence already recorded; additional focused lower-bound red check failed before fix with `BadDecodingError` for pre-Hello `ACKF`/`ERRF` `MessageSize < 8` | Extended the same `tcp_codec.rs` header guard to reject below-header `MessageSize` and added focused pre-Hello ACK/ERR regression coverage; MCP ref OPC-10000-6 7.1.2.2 | `cargo test -p async-opcua-core pre_hello -- --nocapture` passed; `cargo test -p async-opcua-core oversized -- --nocapture` passed; rustfmt and diff checks passed | Scope limited to declared TCP frame-size bounds before splitting/waiting; Hello/Acknowledge negotiation policy remains T077 |
| T077 | T051 red evidence already recorded: the test-only policy-aware shim rejected 1024-byte ECC-intended Hello/Acknowledge buffers through the non-ECC 8192-byte minimum | Added production policy-aware buffer validation in `async-opcua-core/src/comms/tcp_types.rs`; ECC policies use the 1024-byte minimum while default/non-ECC validation remains 8192; MCP ref OPC-10000-6 7.1.2.3 | `cargo test -p async-opcua-core ecc_policy_allows_1024_hello_ack_buffer_sizes_without_weakening_default_minimum -- --nocapture` passed; `cargo test -p async-opcua-core tcp_types -- --nocapture` passed; rustfmt and diff checks passed | Scope is validation API and unit coverage in `tcp_types.rs`; broader transport call-site wiring was not changed |
| T078 | T052 red evidence already recorded; focused pre-fix check failed for a 256-byte asymmetric `SecurityPolicyUri` | Added direct `SecurityPolicyUriLength` decoding and 255-byte validation in `async-opcua-core/src/comms/security_header.rs` before URI allocation/read; MCP ref OPC-10000-6 6.7.2.3 | `cargo test -p async-opcua-core asymmetric_header_decode_rejects_security_policy_uri_longer_than_255_bytes -- --nocapture` passed; `cargo test -p async-opcua-core security_header -- --nocapture` passed; rustfmt and diff checks passed | Scope limited to asymmetric security header URI length; existing certificate/thumbprint validations preserved |
| T079 | T053 red evidence already recorded; focused pre-fix check returned `BadDecodingError` instead of `BadServiceUnsupported` for an unsupported request object id | Updated `async-opcua-core/src/messages/request.rs` to decode the common `RequestHeader` for unsupported service ids and return `BadServiceUnsupported` with request-handle context; routed controller recoverable transport errors through an explicit ServiceFault sender in `async-opcua-server/src/session/controller.rs`; MCP refs OPC-10000-4 5.3 and 7.34 | `cargo test -p async-opcua-core unknown_service -- --nocapture` passed; `cargo test -p async-opcua-core request -- --nocapture` passed; `cargo test -p async-opcua-server unknown_service -- --nocapture` passed with 0 matching tests; rustfmt and diff checks passed | No focused server-side unknown-service integration test matched the filter; reusable-channel behavior is covered by the core context regression and the existing recoverable transport branch |
| T080 | T054 red evidence already recorded; focused pre-fix `max_response` check failed before enforcement | Added per-secure-channel client response-body limit tracking in `async-opcua-core/src/comms/buffer.rs` and refreshed limits from session lifecycle paths in `async-opcua-server/src/session/manager.rs`; normal responses over the cap return `BadResponseTooLarge` while `ServiceFault` remains sendable; MCP refs OPC-10000-4 5.7.2.2 and 5.3 | `cargo test -p async-opcua-server --test security_tests max_response -- --nocapture` passed; `cargo test -p async-opcua-core buffer -- --nocapture` passed; rustfmt and diff checks passed | Enforcement is conservative per SecureChannel; if multiple active sessions share one channel with different nonzero limits, the smallest active limit is applied |
| T081 | T055 red evidence already recorded; focused pre-fix `return_diagnostics` check failed before propagation | Added mutable response-header access and `apply_return_diagnostics` in `async-opcua-core/src/messages/response.rs`; propagated request `returnDiagnostics` through response/fault paths in `async-opcua-server/src/session/controller.rs`; MCP refs OPC-10000-4 7.32 and 7.34 | `cargo test -p async-opcua-server --test service_diagnostics -- --nocapture` passed; `cargo test -p async-opcua-core response -- --nocapture` passed; rustfmt and diff checks passed | Transport-level recoverable faults with only request id/handle still cannot honor `returnDiagnostics` without extending transport metadata |
| T082 | T056 red evidence already recorded; focused pre-fix decode returned `BadDecodingError` before service code could map the value | Replaced generated `MonitoringMode` enum encoding with explicit raw-value-preserving encode/decode in `async-opcua-types/src/generated/types/enums.rs`; invalid values decode as `MonitoringMode::Invalid(i32)` and re-encode unchanged; MCP ref OPC-10000-4 5.13.4.3 | `cargo test -p async-opcua-types monitoring_mode_raw_decode_preserves_invalid_value_for_service_status -- --nocapture` passed; `cargo test -p async-opcua-types monitoring_mode -- --nocapture` passed; `cargo test -p async-opcua-types enums -- --nocapture` passed; `cargo test -p async-opcua-types --all-features monitoring_mode -- --nocapture` passed; rustfmt and diff checks passed | Service-level `BadMonitoringModeInvalid` mapping remains T083 |
| T083 | T057 red evidence already recorded; focused pre-fix integration test returned success instead of `BadMonitoringModeInvalid` | Added a pre-mutation `MonitoringMode::Invalid(_)` guard in `async-opcua-server/src/session/services/monitored_items.rs` returning `BadMonitoringModeInvalid`; MCP ref OPC-10000-4 5.13.4.3 Table 70 | `cargo test -p async-opcua --test integration_tests invalid_monitoring_mode -- --nocapture` passed; `cargo test -p async-opcua-server monitored_item -- --nocapture` passed; rustfmt and diff checks passed | Scope limited to service-level invalid monitoring mode rejection |
| T084a | T058a red evidence already recorded; focused pre-fix test accepted JSON-number Int64 input instead of requiring a decimal string | Updated `async-opcua-types/src/json.rs` so `i64` encodes as a decimal JSON string and decodes only from a JSON string; `basic_types.rs` needed no binary changes; MCP ref OPC-10000-6 5.4.2.3 | `cargo test -p async-opcua-types json_int64_encodes_and_decodes_decimal_string -- --nocapture` passed; rustfmt and diff checks passed | Broader `json` filter still has expected red failures for pending UInt64, NodeId, ExpandedNodeId, Variant, and ExtensionObject tasks |
| T084b | T058b red evidence already recorded; focused pre-fix test accepted JSON-number UInt64 input instead of requiring a decimal string | Updated `async-opcua-types/src/json.rs` so `u64` also uses decimal JSON string encoding and string-only decoding; `basic_types.rs` needed no binary changes; MCP ref OPC-10000-6 5.4.2.3 | `cargo test -p async-opcua-types json_uint64_encodes_and_decodes_decimal_string -- --nocapture` passed; `cargo test -p async-opcua-types json_int64_encodes_and_decodes_decimal_string -- --nocapture` passed; rustfmt and diff checks passed | Broader `json` filter still has expected red failures for pending NodeId, ExpandedNodeId, Variant, and ExtensionObject tasks |
| T085a | T059a red evidence already recorded; focused pre-fix test encoded NodeId as the legacy JSON object instead of OPC UA 1.05 string form | Updated `async-opcua-types/src/node_id/json.rs` to encode/decode NodeId using the Part 6 5.1.12 string form, including namespace URI mapping with namespace-index fallback; adjusted JSON tests in `async-opcua-types/src/tests/json.rs`; MCP refs OPC-10000-6 5.4.2.10 and 5.1.12 | `cargo test -p async-opcua-types json_nodeid_uses_opc_ua_1_05_string_form -- --nocapture` passed; `cargo test -p async-opcua-types serialize_node_id -- --nocapture` passed; `cargo test -p async-opcua-types node_id -- --nocapture` passed; rustfmt and diff checks passed | ExpandedNodeId, Variant, and ExtensionObject JSON work remains pending in T085b-T087 |
| T085b | T059b red evidence already recorded; focused pre-fix test encoded ExpandedNodeId as the legacy JSON object instead of OPC UA 1.05 string form | Updated `async-opcua-types/src/expanded_node_id.rs` to encode/decode ExpandedNodeId using the Part 6 5.1.12 string form, including `svr=` server indexes, `nsu=` namespace URIs, namespace-index mapping when available, and spec fallback for unmapped server URIs; adjusted JSON expectations in `async-opcua-types/src/tests/json.rs`; MCP refs OPC-10000-6 5.4.2.11 and 5.1.12 | `cargo test -p async-opcua-types json_expanded_nodeid_uses_opc_ua_1_05_string_form -- --nocapture` passed; `cargo test -p async-opcua-types serialize_expanded_node_id -- --nocapture` passed; `cargo test -p async-opcua-types expanded_node_id -- --nocapture` passed; `cargo test -p async-opcua-types json_nodeid_uses_opc_ua_1_05_string_form -- --nocapture` passed; rustfmt and diff checks passed | Variant and ExtensionObject JSON work remains pending in T086-T087 |
| T086 | `cargo test -p async-opcua-types json_variant_uses_uatype_and_value_fields -- --nocapture` failed pre-fix because `Variant::Double(1.25)` serialized with legacy `Body` instead of Part 6 `Value` | Updated `async-opcua-types/src/variant/json.rs` to encode Variant JSON as `UaType`/`Value`, preserve `Dimensions`, decode standard `UaType`/`Value`, and isolate legacy `Type`/`Body` and `UaType`/`Body` handling behind `Variant::decode_legacy_type_body_json`; updated Variant JSON expectations in `async-opcua-types/src/tests/json.rs`; MCP refs OPC-10000-6 5.4.2.17 and 5.4.2.18 | `cargo test -p async-opcua-types json_variant_uses_uatype_and_value_fields -- --nocapture` passed; `cargo test -p async-opcua-types json_variant -- --nocapture` passed; `cargo test -p async-opcua-types serialize_variant -- --nocapture` passed; `cargo test -p async-opcua-types variant -- --nocapture` passed; `cargo test -p async-opcua-types node_id -- --nocapture` passed; `cargo test -p async-opcua-types expanded_node_id -- --nocapture` passed; rustfmt and diff checks passed | Full `json` filter still expected to retain pending ExtensionObject work until T087a/T087b complete |
| T087a | `cargo test -p async-opcua-types extension_object_uabody -- --nocapture` failed pre-fix because `UaBody: null` attempted string-body decoding instead of preserving null body semantics | Updated `async-opcua-types/src/extension_object.rs` so `UaBody: null` is consumed explicitly and decodes to `ExtensionObject::null()` after `UaTypeId` validation; adjusted ExtensionObject JSON fixtures in `async-opcua-types/src/tests/json.rs` to current NodeId string form; MCP ref OPC-10000-6 5.4.2.16 | `cargo test -p async-opcua-types extension_object_uabody -- --nocapture` passed; `cargo test -p async-opcua-types serialize_extension_object -- --nocapture` passed; `cargo test -p async-opcua-types extension_object_round_trip -- --nocapture` passed; `cargo test -p async-opcua-types test_binary_in_json -- --nocapture` passed; `cargo test -p async-opcua-types test_xml_in_json -- --nocapture` passed; `cargo test -p async-opcua-types xml_extension_object_in_json -- --nocapture` passed; `cargo test -p async-opcua-types serialize_variant_extension_object -- --nocapture` passed; `cargo test -p async-opcua-types malformed_json_extension_object_no_panic -- --nocapture` passed; rustfmt and diff checks passed | Full `json` filter deferred until T087b duplicate-field work finishes |
| T087b | Existing lock-in `cargo test -p async-opcua-types extension_object_duplicate -- --nocapture` passed; new pre-fix dynamic test showed duplicate `UnitId` decoded successfully with last-write-wins behavior | Updated `async-opcua-types/src/custom/json.rs` to track and reject duplicate JSON field names while decoding dynamic structures and unions loaded through ExtensionObject JSON; added raw-JSON dynamic duplicate-field regression in the same module; MCP ref OPC-10000-6 5.4.2.16 | `cargo test -p async-opcua-types json_dynamic_struct_duplicate_field_names_are_rejected -- --nocapture` passed; `cargo test -p async-opcua-types extension_object_duplicate -- --nocapture` passed; `cargo test -p async-opcua-types custom::json -- --nocapture` passed; `cargo test -p async-opcua-types json -- --nocapture` passed with 56 tests; rustfmt and diff checks passed | Dynamic decode now also rejects duplicate unknown fields inside custom structure/union JSON objects, which is stricter but matches the cited duplicate-field rule |
| T088a | `cargo test -p async-opcua-pubsub --test pubsub_tests invalid_flag -- --nocapture` failed pre-fix because a malformed ExtendedFlags1 PicoSeconds-without-Timestamp datagram was accepted far enough to mutate subscriber state | Updated `async-opcua-pubsub/src/codec/uadp.rs` to reject ExtendedFlags1 PicoSeconds when Timestamp is false during `UadpNetworkMessage::decode`; no `subscriber.rs` change was needed because decode now fails before subscriber mutation; MCP ref OPC-10000-14 7.2.4.4.2 | `cargo test -p async-opcua-pubsub --test pubsub_tests invalid_flag -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test pubsub_tests field_count -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test pubsub_tests -- --nocapture` passed with 6 tests; `cargo test -p async-opcua-pubsub uadp -- --nocapture` passed; rustfmt and diff checks passed | Full pubsub package still has a known pending T089a secured trailing-payload failure |
| T088b | T062b lock-in already passed in the current tree: `field_count = 65535` was rejected with `BadDecodingError` and subscriber state remained unchanged | No production change was needed because `async-opcua-pubsub/src/codec/uadp.rs` already checks `max_dataset_fields` before field allocation and `max_dataset_messages` before writer-id allocation; added a regression in `async-opcua-pubsub/tests/pubsub_tests.rs` for payload-header dataset-message count overflow; MCP refs OPC-10000-14 7.2.4.4.2, 7.2.4.5.2, and 7.2.4.5.5 | `cargo test -p async-opcua-pubsub --test pubsub_tests dataset_message_count -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test pubsub_tests field_count -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test pubsub_tests invalid_flag -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test pubsub_tests -- --nocapture` passed with 7 tests; `cargo test -p async-opcua-pubsub uadp -- --nocapture` passed; rustfmt and diff checks passed | Full pubsub package still has a known pending T089a secured trailing-payload failure |
| T089a | `cargo test -p async-opcua-pubsub --test message_security_tests trailing -- --nocapture` failed pre-fix because a secured payload with authenticated trailing plaintext decoded successfully instead of returning `BadSecurityChecksFailed` | Updated `async-opcua-pubsub/src/security/codec.rs` so secured UADP decode checks that the authenticated/decrypted payload reader is exhausted after decoding the expected NetworkMessage payload, rejecting trailing bytes before replay state can advance; MCP refs OPC-10000-14 7.2.4.4.2, 7.2.4.4.3, and 7.2.4.3 | `cargo test -p async-opcua-pubsub --test message_security_tests trailing -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test message_security_tests oversized -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test message_security_tests decode_round_trips_both_modes -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test message_security_tests -- --nocapture` passed with 13 tests; `cargo test -p async-opcua-pubsub` passed; rustfmt and diff checks passed | T089b remains the explicit oversized-payload task, though its focused regression is already green |
| T089b | T063b lock-in already passed in the current tree: over-limit secured payload returned `BadSecurityChecksFailed` and a subsequent valid message was accepted, proving replay state did not advance | No code change was needed because `async-opcua-pubsub/src/security/codec.rs` already enforces `DecodingOptions::max_secured_payload_len` before secured decode copy/decrypt work, and replay state advances only after decode success in `async-opcua-pubsub/src/engine.rs`; MCP refs OPC-10000-14 7.2.4.4.3.2 and 7.2.4.4.2 | `cargo test -p async-opcua-pubsub --test message_security_tests oversized -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test message_security_tests trailing -- --nocapture` passed; `cargo test -p async-opcua-pubsub --test message_security_tests -- --nocapture` passed with 13 tests; `cargo test -p async-opcua-pubsub` passed; rustfmt and diff checks passed | Confirms the crate's configured secured-payload limit path, not a separate transport/protocol hard-maximum fixture |
| T090 | `cargo test -p async-opcua-server --test security_tests non_current_starting_token -- --nocapture` failed pre-fix because `StartingTokenId=0` returned the earliest retained token `5` instead of the current token `7`. | Updated `async-opcua-server/src/services/security.rs` to separate retained `first_token_id` from `current_token_id`, add an explicit retained-key constructor, route `StartingTokenId=0` to the current key, and preserve exact historical/current/future token range checks with `BadNotFound` for unknown ids; adjusted SKS coverage in `async-opcua-server/tests/security_tests.rs`; MCP ref OPC-10000-14 8.3.2. | `cargo test -p async-opcua-server --test security_tests non_current_starting_token -- --nocapture` passed; `cargo test -p async-opcua-server --test security_tests get_security_keys -- --nocapture` passed; `cargo test -p async-opcua-server --test set_security_keys_tests -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/services/security.rs async-opcua-server/tests/security_tests.rs` passed; `git diff --check -- async-opcua-server/src/services/security.rs async-opcua-server/tests/security_tests.rs` passed. | Preserves the task's no-unverified-fallback constraint: unknown/unavailable token ids still return `BadNotFound`. |
| T091a | Pre-existing malformed-import coverage already exercised the failure path in `async-opcua-xml/src/parser.rs`: local imported XML reaches `root_element_name(&content)?`, which maps `roxmltree::Document::parse` failures to `XmlErrorInner::Xml(_)`. | No code changes required; confirmed the behavior is already grounded in the XML parser path for OPC-10000-6 7.4.3 XML Encoding, with related XML parse-error language checked in OPC-10000-6 5.2.2.8. | `cargo test -p async-opcua-xml malformed_xml_import_exposes_parse_failure -- --nocapture` passed; `cargo test -p async-opcua-xml imports -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-xml/src/parser.rs` passed; `git diff --check -- async-opcua-xml/src/parser.rs` passed. | Full `cargo test -p async-opcua-xml` intentionally deferred because T091b's oversized-import regression was still expected-red at this checkpoint. |
| T091b | `cargo test -p async-opcua-xml oversized_xml_import_is_rejected_before_parsing -- --nocapture` failed pre-fix because an imported 1,048,577-byte local XML file was fully parsed instead of rejected before parsing. | Added a 1 MiB `MAX_IMPORTED_XML_BYTES` guard in `async-opcua-xml/src/parser.rs`; recursive local BSD/XSD imports now check metadata size before reading/parsing while top-level direct XML parsing and remote-import skipping remain unchanged; MCP refs OPC-10000-6 7.4.3 and related XML parse-error context in OPC-10000-6 5.2.2.8. | `cargo test -p async-opcua-xml oversized_xml_import_is_rejected_before_parsing -- --nocapture` passed; `cargo test -p async-opcua-xml malformed_xml_import_exposes_parse_failure -- --nocapture` passed; `cargo test -p async-opcua-xml imports -- --nocapture` passed; `cargo test -p async-opcua-xml -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-xml/src/parser.rs` passed; `git diff --check -- async-opcua-xml/src/parser.rs` passed. | The 1 MiB ceiling is the feature hardening contract, not a numeric OPC UA constant; scope is local imported BSD/XSD files. |
| T092 | `cargo test -p async-opcua-history-sqlite corrupt_event_history_read_reports_data_lost_and_preserves_valid_rows -- --nocapture` failed pre-fix because corrupt stored event decode mapped to `BadInternalError` instead of an explicit HA data-loss status. | Added shared SQLite history blob decode tagging in `async-opcua-history-sqlite/src/query.rs` and mapped read-side corrupt history/event blob decode failures to `StatusCode::BadDataLost` in `async-opcua-history-sqlite/src/backend.rs`; ordinary SQLite failures still map to `BadInternalError`; MCP refs OPC-10000-4 5.11.3 and OPC-10000-11 6.2.2. | `cargo test -p async-opcua-history-sqlite corrupt_event_history_read_reports_data_lost_and_preserves_valid_rows -- --nocapture` passed; `cargo test -p async-opcua-history-sqlite history -- --nocapture` passed; `cargo test -p async-opcua-history-sqlite --test query_migration -- --nocapture` passed; `cargo test -p async-opcua-history-sqlite -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-history-sqlite/src/backend.rs async-opcua-history-sqlite/src/query.rs` passed; `git diff --check -- async-opcua-history-sqlite/src/backend.rs async-opcua-history-sqlite/src/query.rs` passed. | Corrupt rows are surfaced to the caller; no cleanup/delete path is introduced, and the regression confirms valid rows remain. |
| T093a | The duplicate BrowseName regression was already green in the current tree, but the implementation used an unscoped duplicate lookup instead of checking the requested parent relationship precisely; the adjacent T093b type-definition test remained expected-red. | Updated `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` so AddNodes duplicate BrowseName validation searches under the parent using the requested `reference_type_id` before returning `BadBrowseNameDuplicated`; MCP refs OPC-10000-4 5.8.2.4 plus AddNodes per-item result context in 5.8.2.2/5.8.2.3. | `cargo test -p async-opcua-server duplicate_browse_name_returns_operation_level_bad_browse_name_duplicated -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | Broader `memory_mgr_impl` tests still include pending-red T093b/T093c/T094* cases; the duplicate BrowseName case is isolated and passing. |
| T093b | `cargo test -p async-opcua-server invalid_type_definition_returns_operation_level_bad_type_definition_invalid -- --nocapture` failed pre-fix with `Good` instead of `BadTypeDefinitionInvalid` when AddNodes supplied a non-null type definition in an unknown namespace. | Updated `validate_type_definition` in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` so non-null type definitions whose namespace is absent from the address space return `BadTypeDefinitionInvalid`; preserved null/default handling, class checks, abstract type checks, and T093a duplicate BrowseName logic; MCP refs OPC-10000-4 5.8.2.4, 5.8.2.2, and 7.38.2. | `cargo test -p async-opcua-server invalid_type_definition_returns_operation_level_bad_type_definition_invalid -- --nocapture` passed; `cargo test -p async-opcua-server duplicate_browse_name_returns_operation_level_bad_browse_name_duplicated -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | Broader `memory_mgr_impl` tests still include pending T093c/T094* work outside this task. |
| T093c | The focused invalid-attributes path initially returned `BadTypeDefinitionInvalid` because the test fixture lacked the standard `BaseObjectType` namespace/type setup; after isolating that fixture, invalid object attributes needed operation-level `BadNodeAttributesInvalid`. | Added per-node-class `AttributesMask` allowlists in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` so AddNodes rejects known OPC UA attribute bits invalid for the requested `NodeClass`; adjusted the existing invalid-attributes fixture to include namespace 0 and `BaseObjectType`; MCP refs OPC-10000-4 5.8.2.4 and nodeAttributes context in 5.8.2.2/7.24.1. | `cargo test -p async-opcua-server node_attributes_invalid_returns_operation_level_bad_node_attributes_invalid -- --nocapture` passed; `cargo test -p async-opcua-server invalid_type_definition_returns_operation_level_bad_type_definition_invalid -- --nocapture` passed; `cargo test -p async-opcua-server duplicate_browse_name_returns_operation_level_bad_browse_name_duplicated -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | Broader `memory_mgr_impl` tests still include pending AddReferences T094b/T094c work. |
| T094a | `cargo test -p async-opcua-server standard_abstract_reference_type_returns_operation_level_bad_reference_type_id_invalid -- --nocapture` failed pre-fix because standard abstract `References` returned `Good` when the local manager address space did not contain that standard ReferenceType node. | Updated `reference_type_is_abstract` in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` to preserve local `ReferenceType.IsAbstract` lookup and fall back to known standard abstract ReferenceTypes (`References`, `NonHierarchicalReferences`, `HierarchicalReferences`, `HasChild`, `Aggregates`); added a regression for standard abstract `References`; MCP refs OPC-10000-4 5.8.3.2/5.8.3.3/5.8.3.4, OPC-10000-4 7.38.2, and OPC-10000-3 ReferenceType abstraction rules. | `cargo test -p async-opcua-server abstract_reference_type_returns_operation_level_bad_reference_type_id_invalid -- --nocapture` passed; `cargo test -p async-opcua-server standard_abstract_reference_type_returns_operation_level_bad_reference_type_id_invalid -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | T094b duplicate-reference and T094c structural-reference validation remain separate pending tasks. |
| T094b | `cargo test -p async-opcua-server duplicate_reference_returns_operation_level_bad_duplicate_reference_not_allowed -- --nocapture` failed pre-fix with `Good` instead of `BadDuplicateReferenceNotAllowed`; the initially expected shorthand filter without `duplicate` matched no tests in this tree. | Updated `add_references_impl` in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` to normalize requested direction to the stored source/target pair, check `AddressSpace::has_reference` before insertion, and return `BadDuplicateReferenceNotAllowed` without audit/model-change side effects; MCP ref OPC-10000-4 5.8.3.4 plus 5.8.3.2/5.8.3.3 context. | `cargo test -p async-opcua-server duplicate_reference_returns_operation_level_bad_duplicate_reference_not_allowed -- --nocapture` passed; `cargo test -p async-opcua-server abstract_reference_type_returns_operation_level_bad_reference_type_id_invalid -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | T094c structural-reference validation remains a separate pending task at this checkpoint. |
| T094c | `cargo test -p async-opcua-server structural_reference_returns_operation_level_bad_reference_not_allowed -- --nocapture` failed pre-fix with `Good` instead of `BadReferenceNotAllowed` for a `HasProperty` reference whose target node was not a `Variable`. | Updated `add_references_impl` in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` to normalize direction before insertion and reject `HasProperty` references when the structural target is present and not a `Variable`; added `reference_is_structurally_allowed`; MCP refs OPC-10000-4 5.8.3.4, OPC-10000-4 5.8.3.2, and OPC-10000-3 7.8. | `cargo test -p async-opcua-server structural_reference_returns_operation_level_bad_reference_not_allowed -- --nocapture` passed; `cargo test -p async-opcua-server duplicate_reference_returns_operation_level_bad_duplicate_reference_not_allowed -- --nocapture` passed; `cargo test -p async-opcua-server abstract_reference_type_returns_operation_level_bad_reference_type_id_invalid -- --nocapture` passed; `cargo test -p async-opcua-server node_manager::memory::memory_mgr_impl -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | Scoped to the T094c `HasProperty` target-class structural rule, not a complete generic validator for every Part 3 reference-type structural constraint. |
| T095 | `cargo test -p async-opcua-server cross_manager_delete_nodes_delete_target_references_removes_source_manager_reference -- --nocapture` failed pre-fix because DeleteNodes left a stale cross-manager reference in the source manager. | Updated `async-opcua-server/src/session/services/node_management.rs` to run a cross-manager cleanup pass after successful deletes, skipping empty batches; implemented `delete_node_references` in `async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` to mirror local `AddressSpace::delete` reference cleanup and respect `deleteTargetReferences`; added focused in-memory coverage proving source references are cleaned, target references respect the flag, and unrelated references remain; MCP refs OPC-10000-4 5.8.4/5.8.4.1/5.8.4.2/5.8.4.4. | `cargo test -p async-opcua-server cross_manager_delete_nodes_delete_target_references_removes_source_manager_reference -- --nocapture` passed; `cargo test -p async-opcua-server session::services::node_management -- --nocapture` passed; `cargo test -p async-opcua-server node_manager::memory::memory_mgr_impl -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/session/services/node_management.rs async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed; `git diff --check -- async-opcua-server/src/session/services/node_management.rs async-opcua-server/src/node_manager/memory/memory_mgr_impl.rs` passed. | Cross-manager routing still relies on the existing namespace-based `owns_node` heuristic. |
| T096a | `cargo test -p async-opcua-nodes value_rank -- --nocapture` failed pre-fix on positive `ValueRank`/`ArrayDimensions` mismatch cases. | Updated `async-opcua-nodes/src/variable.rs` and `async-opcua-nodes/src/variable_type.rs` so `ValueRank`/`ArrayDimensions` attribute writes and `is_valid()` reject positive-rank dimension-count mismatches with `BadNodeAttributesInvalid`; scalar ArrayDimensions absence was kept for T096b; MCP refs OPC-10000-3 5.6.2 and 5.6.5. | `cargo test -p async-opcua-nodes positive_value_rank_requires_matching_array_dimension_count -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-nodes/src/variable.rs async-opcua-nodes/src/variable_type.rs` passed; `git diff --check -- async-opcua-nodes/src/variable.rs async-opcua-nodes/src/variable_type.rs` passed. | Direct construction paths outside these two owned files, especially generic construction, were not changed in this task. |
| T096b | `cargo test -p async-opcua-nodes array_dimensions_are_rejected_for_scalar_value_rank -- --nocapture` failed pre-fix because both scalar-rank tests returned no error instead of `BadNodeAttributesInvalid`. | Updated `async-opcua-nodes/src/variable.rs` and `async-opcua-nodes/src/variable_type.rs` so `ArrayDimensions` attribute writes and `is_valid()` reject scalar-rank ArrayDimensions while preserving T096a positive-rank dimension-count checks; MCP refs OPC-10000-3 5.6.2 and 5.6.5. | `cargo test -p async-opcua-nodes array_dimensions_are_rejected_for_scalar_value_rank -- --nocapture` passed; `cargo test -p async-opcua-nodes positive_value_rank_requires_matching_array_dimension_count -- --nocapture` passed; `cargo test -p async-opcua-nodes variable -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-nodes/src/variable.rs async-opcua-nodes/src/variable_type.rs` passed; `git diff --check -- async-opcua-nodes/src/variable.rs async-opcua-nodes/src/variable_type.rs` passed. | Generic `new_node_from_attributes` construction remains outside this two-file task scope; enforcement is through the owned setter and validity paths. |
| T097a | `cargo test -p async-opcua --test integration_tests get_endpoints_endpoint_url -- --nocapture` failed pre-fix because GetEndpoints returned the configured/listen URL (`localhost`) instead of the endpoint URL the client used to connect/request (`QuackServer`). | Added a GetEndpoints-specific `ServerInfo::get_endpoints_with_filters` path in `async-opcua-server/src/info.rs` that mirrors the matched request `endpointUrl` into returned `EndpointDescription.endpoint_url`, and updated `SessionController` to use it only for `RequestMessage::GetEndpoints`; MCP refs OPC-10000-4 5.5.4.2 and 7.14. | `cargo test -p async-opcua --test integration_tests get_endpoints_endpoint_url -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests get_endpoints -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/info.rs async-opcua-server/src/session/controller.rs` passed; `git diff --check -- async-opcua-server/src/info.rs async-opcua-server/src/session/controller.rs` passed. | Broader discovery still had the T097b FindServers URL case pending at this checkpoint. |
| T097b | `cargo test -p async-opcua --test integration_tests find_servers_endpoint_url_matches_client_connect_url -- --nocapture` failed pre-fix because FindServers returned `opc.tcp://QuackServer:<port>` instead of the client-used `opc.tcp://QuackServer:<port>/`. | Added `ServerInfo::find_servers_application_description()` in `async-opcua-server/src/info.rs` to mirror a non-empty FindServers request `endpointUrl` into the local server `ApplicationDescription.discovery_urls`, and updated `SessionController` to use it for the local self-description while preserving registered LDS entries; MCP refs OPC-10000-4 5.5.2.2 and 5.5.4.2. | `cargo test -p async-opcua --test integration_tests find_servers_endpoint_url_matches_client_connect_url -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests get_endpoints_endpoint_url -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests discovery -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/info.rs async-opcua-server/src/session/controller.rs` passed; `git diff --check -- async-opcua-server/src/info.rs async-opcua-server/src/session/controller.rs` passed. | Full workspace tests and clippy were not run at this checkpoint. |
| T098a | `cargo test -p async-opcua --test integration_tests localized_text_read_locale -- --nocapture` failed pre-fix because Read returned `en-US/English` instead of the session-preferred `de-DE/Deutsch`. | Added localized attribute variant tracking and read-time session locale selection in `async-opcua-server/src/address_space/utils.rs`, and stored/cleared accepted `ActivateSession.localeIds` by numeric session id in `async-opcua-server/src/session/manager.rs`; MCP ref OPC-10000-4 5.4 Locale Negotiation. | `cargo test -p async-opcua --test integration_tests localized_text_read_locale -- --nocapture` passed; `cargo test -p async-opcua-server session::manager -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/address_space/utils.rs async-opcua-server/src/session/manager.rs` passed; `git diff --check -- async-opcua-server/src/address_space/utils.rs async-opcua-server/src/session/manager.rs` passed. | T098b special-locale write rejection remained pending at this checkpoint; localized-variant cache is guarded by fallback matching, but node deletion does not explicitly purge it in this two-file scope. |
| T098b | `cargo test -p async-opcua --test integration_tests unsupported_special_locale_write_is_rejected -- --nocapture` failed pre-fix because the special-locale write returned `Good` instead of `BadLocaleNotSupported`. | Added LocalizedText attribute write-locale validation in `async-opcua-server/src/address_space/utils.rs` and shared locale normalization/special-locale helpers in `async-opcua-server/src/session/manager.rs`; MCP refs OPC-10000-4 5.4, 5.11.4.1, and 5.11.4.4. | `cargo test -p async-opcua --test integration_tests unsupported_special_locale_write_is_rejected -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests localized_text_read_locale -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests integration::read:: -- --nocapture` passed; `cargo test -p async-opcua-server session::manager -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/address_space/utils.rs async-opcua-server/src/session/manager.rs` passed; `git diff --check -- async-opcua-server/src/address_space/utils.rs async-opcua-server/src/session/manager.rs` passed. | Rejection is scoped to LocalizedText node Attributes (`DisplayName`, `Description`, `InverseName`); OPC UA leaves `Value` Attribute writes with LocalizedText DataType server-specific, so this task did not broaden into Value writes. |
| T099 | `cargo test -p async-opcua --test integration_tests percent_deadband_modify_fetches_eurange_added_after_create -- --nocapture` was already green in the current workspace, proving the intended modify-time EURange behavior was present before this checkpoint. | No code change required; confirmed `async-opcua-server/src/session/services/monitored_items.rs` already resolves PercentDeadband modified item ids to node ids, fetches EURange with `get_eu_range`, and passes ranges into subscription modify; MCP refs OPC-10000-8 7.2 and 7.3.2. | `cargo test -p async-opcua --test integration_tests percent_deadband_modify_fetches_eurange_added_after_create -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests datachange_overflow -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/session/services/monitored_items.rs` passed; `git diff --check -- async-opcua-server/src/session/services/monitored_items.rs` passed. | Full workspace suite deferred to final verification; no T099-specific code edits were made. |
| T100 | `cargo test -p async-opcua --test integration_tests namespace_metadata_property_nodes_are_variables -- --nocapture` failed pre-fix because the dynamic `NamespaceUri` property returned `NodeClass::Object` instead of `NodeClass::Variable`. | Corrected the dynamic NamespaceMetadata property read path in `async-opcua-server/src/diagnostics/node_manager.rs` so property `AttributeId::NodeClass` returns `Variable`; the task text named `async-opcua-server/src/namespace/mod.rs`, but that file only re-exports namespace init helpers and is not the behavior surface; MCP ref OPC-10000-5 6.3.13 Table 22. | `cargo test -p async-opcua --test integration_tests namespace_metadata_property_nodes_are_variables -- --nocapture` passed; `cargo test -p async-opcua --test integration_tests conformance -- --nocapture` passed; `rustfmt --edition 2021 --check async-opcua-server/src/diagnostics/node_manager.rs` passed; `git diff --check -- async-opcua-server/src/diagnostics/node_manager.rs` passed. | Full workspace suite deferred to final verification; conformance filter emits expected negative-path smoke-test logs while passing. |
| T101a | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-core comms` from repo root | Passed with exit code 0; `src/lib.rs` reported 36 passed, 0 failed, 51 filtered out; integration targets `serialization_alloc.rs` and `zero_copy_alloc.rs` ran 0 tests because `comms` was a Cargo test-name filter. | Command is a filtered checkpoint, not full `async-opcua-core`; broader workspace coverage remains in final verification. |
| T101b | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-types json` from repo root | Passed: 56 passed, 0 failed, 0 ignored, 202 filtered out. | Command is a filtered JSON checkpoint, not full `async-opcua-types`; broader workspace coverage remains in final verification. |
| T101c | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-pubsub` from repo root | Passed: 90 total tests passed, 0 failed across library and PubSub integration targets; doc-tests ran 0. | `tsn_jitter` and doc-tests reported 0 tests; broader workspace coverage remains in final verification. |
| T101d | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-server security_tests` from repo root | Passed with exit code 0, but Cargo treated `security_tests` as a test-name filter: all launched targets reported 0 tests; `tests/security_tests.rs` showed 29 filtered out. | This exact task command is a no-op filter in Cargo; meaningful security test target remains `cargo test -p async-opcua-server --test security_tests`, and broader workspace coverage remains in final verification. |
| T101e | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-xml` from repo root | Passed: `src/lib.rs` 8 passed, `tests/import_tests.rs` 3 passed, doc-tests 0; 0 failed. | Broader workspace coverage remains in final verification. |
| T101f | N/A verification-only checkpoint | Ran `cargo test -p async-opcua-history-sqlite` from repo root | Passed: `history_events.rs` 4 passed, `history_update_data.rs` 16 passed, `query_migration.rs` 7 passed, library/doc-tests 0; 27 total passed, 0 failed. | Broader workspace coverage remains in final verification. |
| T102 | N/A documentation reconciliation task | Added final evidence columns to `specs/043-fix-audit-findings/finding-matrix.md` and populated completed rows through T101f from `verification.md`; updated T102's own matrix row during main review. | `rg -n "T098a|T098b|T099|T100|T101a|T101f|TBD before implementation" specs/043-fix-audit-findings/finding-matrix.md` passed; `git diff --check -- specs/043-fix-audit-findings/finding-matrix.md` passed; `rg -n "T102" specs/043-fix-audit-findings/finding-matrix.md specs/043-fix-audit-findings/tasks.md specs/043-fix-audit-findings/verification.md` confirmed ledger alignment. | Documentation-only update; pending T103+ rows intentionally remain pending until their evidence is recorded. |
| T103 | N/A documentation consistency task | Updated `specs/043-fix-audit-findings/quickstart.md` to use the meaningful `cargo test -p async-opcua-server --test security_tests` target instead of the no-op filtered `cargo test -p async-opcua-server security_tests` command. | `rg -n "T098a|T098b|T099|T100|T101a|security_tests|cargo test" specs/043-fix-audit-findings/quickstart.md` passed; `git diff --check -- specs/043-fix-audit-findings/quickstart.md` passed. | Documentation-only update; no test suite was run for this task beyond quickstart consistency checks. |
| T104 | N/A verification-only checkpoint | Ran `cargo fmt --check` from repo root | Passed with exit code 0 and no rustfmt output. | Formatting gate only; full test and clippy gates remain pending. |
| T105 | First full run failed with 10 `async-opcua` integration failures: AddNodes validation returned `BadTypeDefinitionInvalid` before expected operation-level statuses, and write fixtures hit the new scalar `ArrayDimensions` rule; a later rerun exposed libFuzzer targets running indefinitely under `--all-targets --all-features`; the next rerun failed `async-opcua-server::address_space::tests::variable_builder` for `ValueRank=10` with three dimensions | Fixed AddNodes typeDefinition validation to fall back to the shared type tree for standard namespace type nodes; reordered write integration fixtures and corrected the server `variable_builder` fixture to use spec-valid ValueRank/ArrayDimensions; gated fuzz target `no_main`/`fuzz_target!` entry points behind `not(test)` so normal Cargo tests do not start libFuzzer loops | `cargo test --workspace --all-targets --all-features --locked` passed with exit code 0 | Expected negative-path log noise remains in the workspace test output; fuzz targets now run as zero-test harnesses under Cargo test while preserving actual libFuzzer entry points outside `cfg(test)` |
| T106 | First Clippy run failed on `MonitoringMode` `derivable_impls`, then exposed `indexing_slicing` in certificate path-length validation, `cmp_owned` in response-size ServiceFault comparison, `explicit_counter_loop` in GDS cache replacement, `too_many_arguments` in certificate audit dispatch, `unnecessary_map_or` in service diagnostics, and a needless borrow in node-management test code | Derived `Default` for the custom `MonitoringMode` enum while preserving invalid raw decode; replaced unchecked cert-chain slice with checked range lookup; compared ServiceFault `ObjectId` without allocating a `NodeId`; used `enumerate()` for cache rollback count; grouped certificate audit event payload fields; simplified service diagnostics helper; removed needless node-management test borrow | `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` passed with exit code 0 | Clippy gate is clean after focused regression checks for monitoring mode, cert-chain, buffer, GDS/audit, service diagnostics, and node-management paths |
| T106a | N/A verification-only checkpoint | Ran the exact task command; Cargo treated `certificate_audit` as a test-name filter | `cargo test -p async-opcua-server certificate_audit` passed with exit code 0; 2 tests ran and passed, with 443 tests filtered out across launched binaries | Filtered checkpoint only; broader certificate-audit coverage remains represented by T105/T106 and targeted security/audit tests |
| T106b | N/A verification-only checkpoint | Ran the exact task command; Cargo treated `session_audit` as a test-name filter in the `integration_tests` binary | `cargo test -p async-opcua --test integration_tests session_audit` passed with exit code 0; 2 `integration::session_audit::*` tests ran and passed, with 284 filtered out | Filtered checkpoint only; the full integration binary was already covered by T105 |
| T106c | N/A verification-only checkpoint | Ran the exact embedded-profile build command for the minimal server sample | `cargo build --locked --profile embedded -p async-opcua-minimal-server` passed with exit code 0 | Build-only footprint gate; binary size measurement is outside this verification task |
| T106d | N/A verification-only checkpoint | Ran the exact embedded-profile build command for the Foundation nano server sample | `cargo build --locked --profile embedded -p async-opcua-foundation-profile-nano-server` passed with exit code 0 | Build-only footprint gate; binary size measurement is outside this verification task |
| T106e | N/A verification-only checkpoint | Ran the exact embedded-profile build command for the Foundation micro server sample | `cargo build --locked --profile embedded -p async-opcua-foundation-profile-micro-server` passed with exit code 0 | Build-only footprint gate; binary size measurement is outside this verification task |
| T106f | N/A verification-only checkpoint | Ran the exact embedded-profile build command for the Foundation embedded server sample | `cargo build --locked --profile embedded -p async-opcua-foundation-profile-embedded-server` passed with exit code 0 | Build-only footprint gate; binary size measurement is outside this verification task |
| T106g | N/A verification-only checkpoint | Ran the aggregate demo-server interop smoke harness against node-opcua | `./samples/demo-server/interop/run-interop.sh` passed with exit code 0 | Local harness gate only; separate open62541, asyncua, and .NET stack checks remain in T106h-T106j |
| T106h | N/A verification-only checkpoint | Ran the open62541 C-stack demo-server interop harness | `./samples/demo-server/interop/open62541/run-open62541.sh` passed with exit code 0 | Local second-stack harness gate only; asyncua and .NET stack checks remain in T106i-T106j |
| T106i | N/A verification-only checkpoint | Ran the asyncua Python-stack demo-server interop harness | `./samples/demo-server/interop/asyncua/run-asyncua.sh` passed with exit code 0 | Local third-stack harness gate only; .NET reference-stack check remains in T106j |
| T106j | N/A verification-only checkpoint | Ran the .NET OPC Foundation reference-stack demo-server interop harness | `./samples/demo-server/interop/dotnet/run-dotnet.sh` passed with exit code 0 | Local fourth-stack harness gate only; official UACTT remains outside this local verification scope |
| T107a | N/A review/remediation checkpoint; initial review found a medium private-key debug-format exposure in GDS certificate material structs and test snapshots | Replaced derived `Debug` on `GdsCertificateUpdate`, `GdsFinishedSigningRequest`, and `FinishedSigningRequestSnapshot` with redacting formatters, then added `gds_certificate_material_debug_redacts_private_keys` | `cargo test -p async-opcua-server --test gds_pull_methods -- --nocapture`, `cargo clippy -p async-opcua-server --test gds_pull_methods --locked -- -D warnings`, and `cargo fmt --check` passed | Authentication/session token paths had no raw credential/token/signature logging finding; GDS debug output now redacts private-key material, but future callers must still avoid logging returned `Variant::ByteString` private-key outputs |
| T107b | N/A static review checkpoint | Reviewed touched certificate, crypto/GDS, transport, message, and audit paths for panic-on-untrusted-input regressions | Review passed: no untrusted certificate/transport/GDS input panic regression found; notable guarded cases included TCP `split_to`, audit path indexing, certificate parsing, GDS method args, and response truncation | Static review only; tests/fuzzers covered elsewhere and third-party parser internals were not proven panic-free by this pass |
| T107c | N/A static review checkpoint | Reviewed touched PubSub, XML, and history paths for secret logging and panic-on-untrusted-input regressions | Review passed: no touched PubSub/XML/history secret logging or untrusted-input panic regression found | Static review only; existing PubSub key types still derive `Debug`, but no touched log site prints them and this was not introduced by this feature |
| T108 | N/A evidence-consistency audit | Audited `tasks.md`, `verification.md`, and `finding-matrix.md` for checkbox/evidence alignment | T108 worker reported PASS before final checkoff: 167 checked tasks had concrete verification evidence, the only pending row was T108 itself, and T106a-T106j/T107a-T107c were aligned; after marking T108 complete, `cargo fmt --check`, `git diff --check`, `cargo test --workspace --all-targets --all-features --locked`, and `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` all passed | Expected negative-path log noise remains in the full workspace test output; no checkbox/evidence mismatch remains |
